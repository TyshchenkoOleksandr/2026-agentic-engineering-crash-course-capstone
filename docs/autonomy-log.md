# Журнал автономності — Dopamine Clicker

Ведеться **під час роботи**, а не перед здачею. Шаблон — [`templates/autonomy-log.md`](../templates/autonomy-log.md),
що приймається як доказ — [`RUBRIC.md`](../RUBRIC.md).

Рядок на кожну значну частину роботи: **задача → рівень довіри → хто вирішував → докази → чому саме цей рівень.**

Три питання перед кожним рядком:

1. **Як швидко я помічу помилку?**
2. **Наскільки чисто можу відкотити?**
3. **Які докази мене переконають?**

| Рівень | Назва | Хто що робить |
|---|---|---|
| 1 | Асистент | агент пропонує — людина вирішує кожну дію |
| 2 | Асистент → агент | правки агент робить сам, команди — з дозволу; людина не стежить у процесі |
| 3 | Агент | доходить до мети сам і приносить докази |
| 4 | Агенти | паралельні сабагенти, людина зводить результати |
| 5 | Автономні агенти | harness працює без людини, людина — лише за винятком |

Базовий рівень проєкту — **1 · Асистент** (зафіксовано в [`AGENTS.md`](../AGENTS.md)).

---

## Записи

| # | Дата | Робота | Рівень | Хто вирішував | Докази | Чому саме цей рівень |
|---|------|--------|--------|---------------|--------|----------------------|
| 1 | 2026-09-17 | Каркас Next.js 16 + скіли (vercel-react-best-practices, caveman) | 2 · Асистент → агент | людина обрала стек і скіли; агент виконав і закомітив (`Co-Authored-By: Claude` у всіх трьох комітах) | `7037369`, `d00c324`, `815a8cd` (до появи `.agent-log` — журналу дій немає) | незворотний вибір стеку — рішення за людиною, виконання за агентом |
| 2 | 2026-09-17 | Харнес із day01: hooks (`protect-env`, `log-action`), правила, MCP | 2 · Асистент → агент (журнал: `mode: auto`) | людина вирішила взяти харнес із day01; агент скопіював файли, сам додав `@types/node` (`pnpm add -D`), прогнав `pnpm check` і `hooks:selftest`, закомітив | `8215302`; `.claude/settings.json`; `.agent-log` сесія `0fe1275f` | зміна харнесу мала йти на рівні 1, фактично агент працював в auto — див. «Зниження» |
| 3 | 2026-09-17 | OpenSpec + бриф проєкту | 2 · Асистент → агент | людина придумала ідею Dopamine Clicker і погодила правила в обговоренні (бриф: «agreed in discussion»); агент встановив `@fission-ai/openspec`, написав `docs/project-brief.md`, закомітив | `d14a51d`, `d75edfe`; сесії `0fe1275f`, `6a870f91` | специфікація визначає все далі — зміст за людиною, оформлення за агентом |
| 4 | 2026-09-17 → 09-19 | Playwright, `tests:locked`, 7 сабагентів, red → green у `openspec/config.yaml` | 2 · Асистент → агент (журнал: `mode: auto`) | людина вирішила вимагати e2e і red → green, відповіла на `AskUserQuestion` агента (09-19); агент додав Playwright, написав `tests-locked`, `playwright.config.ts`, `openspec/config.yaml`, правив `AGENTS.md` | `a2cf436`; сесії `fb6fb368`, `a2564276`, `73d98bff`; `pnpm check` зелений (6 тестів) | конфіг збірки і тестів мав іти на рівні 1, фактично агент працював в auto — див. «Зниження» |
| 5 | 2026-09-19 | Аудит сетапу проти `RUBRIC.md` | 1 · Асистент | агент лише читав і запускав `pnpm check`; що робити далі вирішує людина | звіт у сесії; `.agent-log/actions.jsonl` | агент нічого не змінював — ризику немає |
| 6 | 2026-09-19 | Ключ Context7 для MCP | 1 · Асистент | агент написав скрипт `setup-context7.sh`; ключ вводила людина сама у своєму терміналі; людина вирішила видалити скрипт після налаштування | `.mcp.json` (`${CONTEXT7_API_KEY}`); `claude mcp list` → `context7 ✔ Connected` | секрети — завжди рівень 1; ключ ніколи не проходив через агента |
| 7 | 2026-09-19 | Сабагент `autonomy-logger` + заповнення журналу за доказами | 1 · Асистент | людина попросила журнал і сабагента; агент заповнив «Хто вирішував» лише з git (`Co-Authored-By`) і `.agent-log`, а де доказів немає — лишив `_людина: підтвердити_` | `.claude/agents/autonomy-logger.md`; рядки 1–4 цього журналу | зміна харнесу; агент сам виявив розбіжність рівня (див. «Зниження») |
| 8 | 2026-09-19 | Цикл `pnpm agent:loop <change>` для green-фази | 1 · Асистент (побудова); сам цикл — 3 · Агент | людина вимагала, щоб цикл був частиною розробки, а не окремою вправою → агент привʼязав його до OpenSpec (старт лише після `test(<change>):` коміту); людина двічі відхилила unit-тест із фейковим агентом → тест видалено | `scripts/agent-loop.mjs`; `AGENTS.md`, `openspec/config.yaml`; коміт `6ba5f2a` | цикл працює без нагляду (рівень 3), бо зупинку гарантують `pnpm check` + `tests:locked` + ліміт 5 ітерацій; реального прогону ще немає |
| 9 | 2026-09-19 | Автозапуск циклу git-hook'ом після red-коміту | 1 · Асистент | агент спробував сам виконати `git config core.hooksPath` — заблоковано класифікатором auto mode (`Unauthorized Persistence`); людина обрала варіант «агент пише файл, вмикаю я» і сама ввімкнула hook | `.githooks/post-commit`; `git config core.hooksPath` = `.githooks`; заблокована дія в `pnpm agent:log` | запуск агента без натискання клавіші — рішення лише за людиною |
| 10 | 2026-09-19 | Мутаційне тестування Stryker для `lib/` | 1 · Асистент | людина погодила `pnpm add -D` (правило AGENTS.md); агент налаштував, пройшов 3 помилки (symlink у sandbox, плагін не знайдено під pnpm, 49 lint-помилок від залишків sandbox) і виправив без вимкнення правил lint | `stryker.config.mjs`; пробний прогін: 80 %, 1 мутант вижив (`>=` → `>`), тимчасові файли видалено; `pnpm check` зелений | нова залежність — рівень 1 за `AGENTS.md` |
| 11 | 2026-09-19 | Коміт усього сетапу | 1 · Асистент | людина сказала «просто закоміть» одним комітом (агент пропонував 5 логічних) | `6ba5f2a` (23 файли) | коміт — з явної команди людини |
| 12 | 2026-09-19 | Stage 1 (`add-foundation`): OpenSpec draft (спецifікації, `design.md`, `tasks.md`) сабагентом `spec-writer`; людина змінила D4 | 4 · Агенти (сабагент) | людина довірила чернетку специфікації агенту; агент (`Agent` toolcall 10:40:13–10:47:44) написав `proposal.md`, 4 spec-файли, `design.md`, `tasks.md`; людина відхилила початковий D4 і зажадала бекап пошкодженого сейву — агент переписав спек | сесія `adf32e82`, `.agent-log/actions.jsonl` (Write 10:43–10:47); `openspec/changes/add-foundation/tasks.md:12` — «0.1 [human] Confirm the DECISION items… confirmed — D4 changed to add the `dopamine-clicker:save:bad` backup, specs updated»; `design.md:61` «D4 … confirmed (backup changed by human)» | зміст рішення — за людиною (D4 явно позначено як її зміну), оформлення спеки — агентом |
| 13 | 2026-09-19 | Stage 1: червоні тести `lib/game` (game-logic), `lib/i18n` (i18n), `e2e/add-foundation.spec.ts` (e2e-qa) — три сабагенти паралельно | 4 · Агенти | людина попросила red→green за `openspec/config.yaml`; агент запустив 3 сабагенти одночасно (`Agent` toolcalls 10:57:24 / 10:57:29 / 10:57:34, завершення 11:00:01 / 11:00:31 / 11:01:30); людина рішень по вмісту тестів не приймала | комміт `338dd80 test(add-foundation): add failing tests`; сесія `adf32e82`: паралельні `Agent`-виклики з накладеними інтервалами; файли `lib/game/*.test.ts` (heredoc через Bash, `cd lib/game && cat > click-value.test.ts`), `lib/i18n/i18n.test.ts` (Write/Edit 10:58), `e2e/add-foundation.spec.ts` (Write 11:00:12); `tasks.md` §0 — 92 failed / 12 passed Vitest, 27 failed / 1 passed Playwright після цього кроку | три незалежні сабагенти одночасно писали тести в різних теках без взаємного перекриття — паралельна робота, людина звела лише результат (комміт) |
| 14 | 2026-09-19 | Green-фаза `pnpm agent:loop add-foundation` — реальний перший прогін (автостарт git-hook'ом після red-коміту) | 3 · Агент (журнал: `mode: auto`) | людина раніше вирішила прив'язати цикл до red-коміту (рядок 8) і ввімкнути hook (рядок 9); цей прогін стартував сам після `338dd80`, без натискання клавіші людиною; агент за 1 ітерацію сам реалізував усі owner-зони тижня (`[game-logic]`, `[i18n]`, `[ui-frontend]`, `[fx-animations]`) — не лише свою | `.agent-log/loops/2026-09-19T11-06-38-900Z-add-foundation.jsonl`: iteration 0 check ✘ (12 passed/92 failed, e2e 1/27) → iteration 1 agent 795124 ms → check ✔ (106/106, e2e 28/28) → `stop reason: green, iterations: 1`; сесія `3a55385b` (184 дій); `.agent-log/loops/latest.out` | цикл довів до зеленого сам, без нагляду під час виконання — рівень 3 підтверджено першим реальним прогоном (див. «Зміни рівня → Підвищення», рядок 8) |
| 15 | 2026-09-19 | Той самий прогін циклу: мутаційне тестування (`pnpm test:mutation`) заблоковане — не у allow-list циклу | 1 · Асистент (межа зафіксована конфігом циклу, не людиною в моменті) | агент циклу спробував виконати завдання 1.7/5.3, але `pnpm test:mutation` не в allow-list non-interactive циклу — команду відхилено; агент лишив пункти невиконаними і написав про це в звіті | `.agent-log/loops/latest.out`: «Not done, needs you: 1.7 / 5.3 `pnpm test:mutation` — command not allow-listed, run denied in this non-interactive loop. Mutation score unverified.» | команда поза allow-list — це постійна межа з `AGENTS.md`/`.claude/settings.json`, цикл сам зупинився і не намагався обійти |
| 16 | 2026-09-19 | Рев'ю сабагентом (`reviewer`) diff'а після green-фази: зауваження до dialog a11y, ігрового стору, pre-paint скриптів, мутаційного тестування | 3 · Агент | людина попросила review перед комітом («Review the diff, run the reviewer agent, then commit it yourself» — інструкція самого циклу); агент-рев'юер сам прочитав усі файли, прогнав `pnpm test:mutation`, `pnpm check`, `pnpm test:e2e`, знайшов, що мутаційний скор — 14 % на vitest 5 (кілерабельні мутанти показані як survivors) | сесія `adf32e82`, `Agent` toolcall 11:30:51–11:34:49 (`pnpm test:mutation` 11:30:53, `pnpm check` 11:31:00, `pnpm test:e2e` 11:31:17, читання `components/*`, `e2e/add-foundation.spec.ts`, `design.md` D7/D8/D10/D11); коміт `6e2c3f8`: «score 14% with killable mutants reported as survivors»; коміт `2fc7d33`: «reviewer fixes (dialog a11y name, real game store, pre-paint scripts built from lib constants, role=status counter, in-memory preference fallback, fewer 'use client')» | агент сам довів до знахідок і доказів (виконані команди, конкретні відсотки) — людина лише ініціювала крок «review, then commit yourself» |
| 17 | 2026-09-19 | Пін `vitest` `^5` → `^4.1.11` для мутаційного тестування (`pnpm add -D vitest@^4`) | 1 · Асистент (за `AGENTS.md`); журнал: `mode: auto`, без зафіксованого `AskUserQuestion` | за завданням постановника: людина погодила пін vitest до версії 4 (скор став 94.56%); агент сам виконав `pnpm add -D vitest@^4` і закомітив | сесія `adf32e82`, Bash 11:47:15 `pnpm add -D vitest@^4`; коміт `6e2c3f8` (автор Oleksandr Tyshchenko, `package.json` `"vitest": "^4.1.11"`, `stryker.config.mjs` `tempDirName: ".stryker-tmp"`); `tasks.md:32` — «94.56% after `chore: fix mutation testing`» | **розбіжність**: залежність — завжди рівень 1 за «Постійні межі», але `.agent-log` не містить `AskUserQuestion` чи явного підтвердження людини перед `pnpm add` у цій сесії — самé схвалення відоме лише з постановки задачі, а не з журналу дій; див. «Зниження» |
| 18 | 2026-09-19 | Сабагент `ui-frontend` застосував правки рев'ю: `game-store.ts`, `GameScreen.tsx`, `BalanceCounter.tsx`, `app/layout.tsx`, `ResetProgress.tsx`, менше `'use client'` | 3 · Агент | людина рішень по конкретних правках не приймала (крок циклу «review → fix» без паузи); агент сам прочитав усі компоненти (11:36:08–11:36:39), тоді написав/відредагував їх (11:38:36–11:41:31), додав 3 нових тест-файли (`BalanceCounter.test.tsx`, `game-store.test.ts`, `preferences-store.test.ts`) | сесія `adf32e82`, `Agent` toolcall 11:36:04–11:36:06 (диспетч) + Write/Edit 11:38:36–11:41:31; локальний `pnpm check` 11:46:18 → зелений; коміт `2fc7d33` (119/119 Vitest) | агент сам довів правки до зеленого `pnpm check` перед тим як передати керування далі — доказ є, ризик локальний (компоненти, не харнес) |
| 19 | 2026-09-19 | E2E заблоковано «завислим» `next start` на порту 3000 у терміналі людини; людина зупинила процес | 1 · Асистент (агент лише діагностував, не чіпав чужий процес) | агент дослідив (`lsof -i :3000`, `ps -o pid,ppid,lstart,command -p 68433/68447/19961`) і не вбивав процес — не у allow-list; `pnpm test:e2e` о 11:42–11:44 не міг використати порт; фінальний прогін о 12:07:34 (`npx playwright test` 12:07:16, `pnpm test:e2e` 12:07:34) — 28 passed, отже порт звільнила людина між 11:51 і 12:07 | сесія `adf32e82`: Bash 11:41:15…11:45:51 (`lsof`, `ps`) без жодної команди `kill`; повторний успішний запуск 12:07:34–12:07:49; коміт `2fc7d33`: «pnpm test:e2e: 28 passed» | чужий процес (не запущений і не власний агента) — агент лише звітує, зупинка стороннього процесу завжди за людиною |
| 20 | 2026-09-19 | Фінальний комміт Stage 1: `feat(add-foundation)` — тема, мова, головна кнопка, персистентність, скидання | 2 · Асистент → агент | людина явно вирішила: red→green правило `AGENTS.md`, `pnpm check`/`test:e2e`/`test:mutation` як Definition of Done; агент сам зібрав `git add`/`git commit` після зеленого `pnpm check` (119/119), `pnpm test:e2e` (28/28) і `pnpm test:mutation` (94.56%) | коміт `2fc7d33` (автор Oleksandr Tyshchenko, `Co-Authored-By: Claude Opus 5`); сесія `adf32e82`, Bash 12:08:09–12:08:11 (`git add app components lib openspec …`, коміт слідом, за часом збігається з `2fc7d33` 15:08:11 +0300 = 12:08:11 UTC) | коміт — фіксація вже узгодженого й перевіреного результату, за правилом Definition of Done з `AGENTS.md`; сам факт коміту без окремого дозволу на кожен — узгоджена практика (рядок 11) |
| 21 | 2026-09-19 | Архівування `add-foundation` і синхронізація основних спек (`openspec/specs/*`) | 1 · Асистент (журнал: `mode: auto`) | агент підготував архів (`openspec list/status/instructions archive` 12:30:56) і поставив `AskUserQuestion` (12:31:14 → відповідь 12:40:57), після відповіді сам зробив `mv` в `archive/` і коміт; зміст відповіді людини в `.agent-log` не записано — _людина: підтвердити_ | коміт `d8fb479` (14 файлів, +691); сесія `adf32e82`, Bash 12:41:17–12:41:19; `AskUserQuestion` 12:31:14/12:40:57 | перенесення спек у «джерело правди» — з питанням людині перед дією |
| 22 | 2026-09-19 | Stage 2 (`add-shop-v1`): чернетка OpenSpec (proposal, 9 spec-файлів, `design.md`, `tasks.md`) сабагентом `spec-writer`; людина змінила D6 | 4 · Агенти (сабагент) | агент (`spec-writer`) написав чернетку з DECISION-пунктами; головний агент передав йому правку через `SendMessage` (13:00:57); людина підтвердила всі DECISION, крім D6: округлення ціни змінено на `Math.round(Number((base * 1.15 ** owned).toFixed(6)))` → 2-га мавпа коштує 58, а не 57 (запис у `tasks.md` 0.1 і `design.md` D6). Кількість рішень: `design.md` має 16 позначок `DECISION` (D1–D20 без D10, D14, D16) + 2 окремо підтверджені пункти (reset як зона без декору, накладання після resize); постановник рахує 20 — _людина: підтвердити_ число | сесія `adf32e82`: `Agent` 12:41:32–13:00:06, `SendMessage` 13:00:57, `SubagentHandback` 13:01:58; `openspec/changes/archive/2026-09-19-add-shop-v1/tasks.md:7-9, :14`; `design.md:33-35, :75` «D6 … confirmed, changed by human»; коміт `ccdfabb` | зміст рішень — за людиною (D6 явно її зміна), оформлення специфікації — агентом |
| 23 | 2026-09-19 | Stage 2: червоні тести — `game-logic` (`lib/game/*`), `i18n` (`lib/i18n/*`), `e2e-qa` (`e2e/add-shop-v1.spec.ts`) паралельно; `e2e-qa` впав і був перезапущений | 4 · Агенти | агент запустив 3 сабагенти одночасно (`Agent` 13:02:20 / 13:02:26 / 13:02:30); два завершились (13:08:09, 13:08:58), третій — `PostToolUseFailure`, `exit: error` через 391 с (13:09:04); перезапуск `Agent` 14:48:18–15:13:24 (1505 с). Причина збою (ліміт сесії Sonnet) і перезапуск на Opus — за словами постановника; у `.agent-log` модель і текст помилки не записані | сесія `adf32e82`, `.agent-log/actions.jsonl` (Agent 13:02–13:09, 14:48–15:13); `pnpm agent:log` → `Agent … failed 1`; коміт `ccdfabb` (32 файли, +4919/−183, зокрема `e2e/add-shop-v1.spec.ts` +956) | три незалежні сабагенти в різних теках; людина звела лише результат (коміт) |
| 24 | 2026-09-19 | Сабагент `i18n` вийшов за межі задачі: правка `lib/i18n/index.ts` (не його файл за `tasks.md` 0.11) і залишений `index.ts.bak` | 4 · Агенти (помилка сабагента, виправлена головним агентом) | агент `i18n` виконав `sed -i.bak '36s/…/…/' lib/i18n/index.ts` (13:05:11, заміна рядка на такий самий — no-op) і переписав файл через `cat > lib/i18n/index.ts` (13:05:33); головний агент перевірив diff проти `HEAD` і `.bak` (14:47:50–14:48:00) і відкотив: `git restore lib/i18n/index.ts && rm lib/i18n/index.ts.bak` (14:48:05, exit 0). Чи бачила це людина в процесі — _людина: підтвердити_ | сесія `adf32e82`, Bash 13:05:11, 13:05:33, 14:47:50, 14:48:05; `pnpm test -- lib/i18n` двічі з exit 1 (13:04:10, 13:06:21 — очікувано для red); `lib/i18n/index.ts` відсутній у `ccdfabb` | сабагент не мав права на файл поза своєю зоною — виявив і відкотив головний агент, а не людина |
| 25 | 2026-09-19 | Red-коміт `test(add-shop-v1): add failing tests` → автостарт `pnpm agent:loop` git-hook'ом | 2 · Асистент → агент (журнал: `mode: auto`) | агент сам виконав коміт, хоча в `tasks.md` 0.15 він позначений `[human]`; перша спроба з `AGENT_LOOP=off` (15:16:55, exit 0, але коміту в `git reflog` немає), друга — без `AGENT_LOOP=off` (15:17:05) → коміт `ccdfabb` 15:17:07 UTC і цикл стартував о 15:17:07 сам. Хто вирішив комітити і вмикати цикл саме зараз — _людина: підтвердити_ | коміт `ccdfabb`; `git reflog` (одна red-комміт-подія 18:17:07 +0300); сесія `adf32e82`, Bash 15:16:55, 15:17:05; `.agent-log/loops/2026-09-19T15-17-07-718Z-add-shop-v1.jsonl` → `phase: start`, `redCommit: ccdfabb` | коміт за правилом red → green із `openspec/config.yaml`; автостарт — заздалегідь погоджений механізм (рядок 9) |
| 26 | 2026-09-19 | Green-фаза `pnpm agent:loop add-shop-v1`: зелено за 2 ітерації | 3 · Агент (журнал циклу: `mode: acceptEdits`) | цикл працював без людини; ітерація 1 реалізувала все, крім 1 unit-тесту; ітерація 2 закрила його, обійшовши суперечливий locked-тест (ASCII `'` у `i18n.test.ts:145` проти U+2019 у D18 і e2e): словник з ASCII, а `t()` конвертує `'` → U+2019 (`Edit lib/i18n/index.ts` 15:36:06). Цикл тричі пробував `pnpm test:mutation` (15:32:25/27/29) — `PreToolUse` без `PostToolUse`, тобто не виконано (поза allow-list) | `.agent-log/loops/2026-09-19T15-17-07-718Z-add-shop-v1.jsonl`: iteration 0 check ✘ (98/138, e2e 23/51) → iteration 1 agent 891379 ms → check ✘ (241 passed / 1 failed, e2e 74/0) → iteration 2 agent 107879 ms → check ✔ (242/0, e2e 74/0) → `stop: green, iterations: 2`; сесія `f42a8267` (261 дія); `tasks.md` 2.1 (опис обходу), 5.3 «Not run: … not on the agent-loop allow-list» | цикл дійшов до зеленого сам, але `tests:locked` не спіймав обхід — змінено код, а не тест; це виявив лише рев'ю (рядок 27) |
| 27 | 2026-09-19 | Рев'ю (`reviewer`) виявив обхід locked-тесту; людина обрала follow-up red-коміт замість конверсії в `t()` | 1 · Асистент (зміна locked-тесту — рішення людини); журнал: `mode: auto` | сабагент-рев'юер (фоновий `Agent` 15:37:52, `SubagentHandback` 15:40:54) позначив конверсію в `t()`; за словами постановника, людина погодила новий red-коміт, що виправляє очікування тесту на U+2019 — у `.agent-log` немає `AskUserQuestion` на це (рішення в чаті). Агент виправив тест (`sed … 145s/М'яка/М’яка/`, коміт з `AGENT_LOOP=off`, 16:03:46), поставив U+2019 у `dictionaries.ts` і прибрав конверсію з `index.ts` (16:04:01) | коміти `95e83ce` («contradicting design D18 and the e2e test, which require U+2019»), `866f6c3` (без `lib/i18n/index.ts` — обхід циклу не потрапив у історію); `tasks.md` 0.15 «follow-up red commit `95e83ce`»; `tasks.md` 5.1 — `ok add-shop-v1: 11 locked test file(s) unchanged since 95e83ce` | зміна locked-тесту — саме те, від чого `tests:locked` захищає; тому лише з рішення людини і окремим `test(...)` комітом |
| 28 | 2026-09-19 | `eslint.config.mjs` і `vitest.config.mts`: ігнор `.stryker-tmp/**` | 1 · Асистент (за `AGENTS.md`); журнал: `mode: auto`, без зафіксованого `AskUserQuestion` | за словами постановника, людина погодила правку ask-first файлу; агент сам вніс правку (`python3` 16:04:15–16:04:25) і закомітив окремим `chore:`. Причина з коміту: `pnpm check` падав на lint і подвійно рахував тести з `.stryker-tmp/` під час прогону мутацій | коміт `7fad809` (2 файли, +4/−2); сесія `adf32e82`, Bash 16:04:08 (читання), 16:04:15 (зміна); пробний `vitest run --exclude '.stryker-tmp/**'` 15:38:53 | **розбіжність**: `eslint.config.mjs` — «Постійні межі», рівень 1; схвалення відоме лише з постановки задачі, не з журналу дій — див. «Зниження» |
| 29 | 2026-09-19 | Правки рев'ю сабагентом `ui-frontend` (зона балансу вільна від декору, форматовані лічильники, зсув cap-hop, клавіатурний +N, `aria-describedby` у магазині, очищення таймерів) | 4 · Агенти | агент `ui-frontend` сам вніс правки (`Agent` 16:04:36–16:07:16, `SubagentHandback` 16:07:12); головний агент після цього прогнав `pnpm check` і `pnpm test:e2e` (16:07:20–16:09:31). Рішень людини щодо конкретних правок у журналі немає | коміт `866f6c3` (20 файлів, +1683/−170; список правок у повідомленні коміту); `tasks.md` 5.1 — `pnpm check` exit 0, 248/248 Vitest (17 файлів), lint 0 errors / 2 warnings у locked `i18n.test.ts`; 5.2 — `pnpm test:e2e` exit 0, 74 passed | локальні правки UI, доведені агентом до зелених перевірок |
| 30 | 2026-09-19 | Мутаційне тестування `lib/`: 95.07 % | 2 · Асистент → агент (журнал: `mode: auto`) | головний агент запустив `pnpm test:mutation` сам (15:37:53–15:39:45), бо цикл не мав права (рядок 26) | `tasks.md` 1.9, 5.3 — exit 0, 95.07 % (769 killed, 2 timeout, 38 survived, 2 no coverage); коміт `866f6c3` «pnpm test:mutation: 95.07%». Перший прогін 15:37–15:39 (до правок `ui-frontend`) дав 94.93 %; 95.07 % — повторний прогін ≈16:09 UTC після правок, у тому ж Bash-виклику, що й `pnpm check` + `pnpm test:e2e`. Хук не записав цей виклик в `.agent-log` (доказ лише у звіті Stryker, 19:09:30 за місцевим часом) — прогалина спостережуваності. Застарілий рядок «Not run» у 5.3 прибрано | команда з allow-list основної сесії; поріг 70 % перевіряє сам Stryker |
| 31 | 2026-09-19 | Візуальна перевірка (task 5.4), коміт `feat(add-shop-v1)` і архівування зміни | 1 · Асистент (5.4 — людина) | людина зробила візуальну перевірку (обидві теми, обидва режими руху) і дала OK у чаті — за словами постановника, в `.agent-log` відповіді немає; агент позначив 5.4 (`sed` 16:10:45), закомітив `feat` (16:11:00), прогнав `openspec validate --specs --strict` і `pnpm tests:locked` та закомітив архів (16:11:07) | коміти `866f6c3`, `69ac859` (21 файл, +1041/−84); сесія `adf32e82`, Bash 16:10:45, 16:11:00, 16:11:07; `tasks.md` 5.4 `[x]` | візуальну якість не перевіряє жоден тест — рішення лише за людиною |
| 32 | 2026-09-19 | Stage 3 (`add-upgrades-v2`): чернетка OpenSpec (proposal, 9 spec-файлів, `design.md`, `tasks.md`) сабагентом `spec-writer`; людина підтвердила D1–D8, D10–D12, D15, D20, D21 як запропоновано, змінила D9 (без округлення — дробовий carry по кліку) і вирішила три відкриті питання (тест-хук `globalThis.__dcRandom` замість підміни `Math.random`, screen shake на окремому шарі замість `<main>`, укр. «5 %» з U+00A0) | 4 · Агенти (сабагент) | агент (`spec-writer`) написав чернетку (`Agent` 16:48:38→17:11:50, 1 391 050 мс); головний агент передав правку через `SendMessage` (17:19:08); сабагент повернув перероблену специфікацію (`SubagentHandback` 17:25:12); зміст рішень — за людиною | сесія `adf32e82`; `.agent-log/actions.jsonl` (`Agent` 16:48:38 / `SendMessage` 17:19:08 / `SubagentHandback` 17:25:12); `design.md:56-61` — «All items marked DECISION (confirmed) were confirmed by the human on 2026-09-19… D9 was changed… The open questions were resolved as: test-only random hook (D19), shake on a dedicated layer (D12, D18), Ukrainian percentages with a no-break space (D21)…»; `tasks.md:9-14, 0.1` — «confirmed 2026-09-19; D9 changed to the main-click carry, open questions resolved»; готова специфікація потрапила в коміт `12506f8` разом з тестами (окремого чернеткового коміту немає) | зміст рішень — за людиною (D9 явно її зміна, відкриті питання явно вирішені нею), оформлення специфікації — сабагентом |
| 33 | 2026-09-19 → 09-20 | Stage 3: червоні тести `game-logic` / `i18n` / `e2e-qa` паралельно, двічі: перша спроба (17:25) обірвана лімітом сесії Opus для всіх трьох, друга (05:26) — один сабагент зник мовчки і був перезапущений, `i18n` відновився через `git restore` | 4 · Агенти | перший запуск (`Agent` 17:25:31 / 17:25:35 / 17:25:39) дав один явний збій (`PostToolUseFailure`, 68 923 мс) і два підозріло короткі «успіхи» (32 383 мс, 45 310 мс — задорого мало для реального написання тестів, як показав повторний запуск нижче); після ~12 год паузи другий запуск (05:26:51 / 05:26:57 / 05:27:03) — два сабагенти завершились штатно (287 480 мс, 524 294 мс), третій (05:26:51) не отримав жодного `PostToolUse` і був перезапущений окремо (05:40:38→05:47:17, 397 064 мс); хто саме вирішив зачекати ввечері і дозволив ранковий перезапуск — _людина: підтвердити_ (у журналі дій підтверджень немає, лише розрив у часі) | сесія `adf32e82`, `.agent-log/actions.jsonl`: `Agent` `toolu_01WKT3Ta7fwiVWLN8J4RNuK3` → `PostToolUseFailure` 17:26:45 (exit error, 68 923 мс), `toolu_014wmGDLtmhvead4Eaz4vYey` → 17:26:18 (45 310 мс), `toolu_01Tdv7kqvKea4zNo4RBZUXgz` → 17:26:13 (32 383 мс); `toolu_01D5AmNESY8pgwgdgsFg1ErA` 05:26:51 — лише `PreToolUse`, без `PostToolUse` (єдиний «blocked» рядок для `Agent` у `pnpm agent:log` за 2026-09-20); `Bash` 05:28:14 `git restore lib/i18n/dictionaries.ts lib/i18n/i18n.test.ts && git status --porcelain lib/i18n/`; `toolu_01RRifWN3wBAMAZs1QP8bagm` 05:40:38→05:47:17; коміт `12506f8` (05:51:45 UTC); `tasks.md` 0.14–0.17 | три незалежні сабагенти в різних теках; мовчазна втрата одного сабагента і факт двох спроб підтверджені журналом дій, рішення «повторити вранці» — поза журналом |
| 34 | 2026-09-20 | Green-фаза `pnpm agent:loop add-upgrades-v2`: 2 ітерації, зупинка `no-progress` з 1 тестом, що не пройшов — це виявився хибний locked-тест, а не недороблена реалізація | 3 · Агент (журнал циклу: `mode: acceptEdits`) | цикл сам довів `pnpm check` / `pnpm test:e2e` з 188/168 (e2e 26/87) до 364/1 (e2e 113/0) за 1-шу ітерацію (16 хв); 2-га ітерація (21 с) нічого не змінила — `check` дав той самий результат, тому цикл сам зупинився, не намагаючись обійти тест | `.agent-log/loops/2026-09-20T05-51-46-170Z-add-upgrades-v2.jsonl`: iteration 0 check ✘ (188 passed/168 failed, e2e 26/87) → iteration 1 agent 975 856 мс → check (364 passed/1 failed, e2e 113/0) → iteration 2 agent 21 193 мс → check той самий результат (364/1, e2e 113/0) → `stop: no-progress, iterations: 2`; сесія циклу `01394624` | цикл не почав гадати чи правити код у 2-й ітерації без прогресу — це і є детектор у дії; єдиний тест, що лишився червоним, виявився хибним самим тестом (рядок 35), а не пропущеною роботою |
| 35 | 2026-09-20 | Два follow-up red-коміти виправляють самі тести, не реалізацію: хибна IEEE-754 передумова в тесті carry (`0.7 + 0.3` і `0.9 + 0.1` насправді рівно 1, а не з похибкою) і вимірювання crit-ефекту двома окремими `boundingBox()` в різні кадри 300-мс тряски (6 з 8 прогонів падали) | 1 · Асистент (зміна locked-тесту — за прецедентом рядка 27); `AskUserQuestion` за весь Stage 3 не зафіксовано жодного разу | агент сам діагностував обидва випадки і виправив тести одразу після зупинки циклу (`af6426a` — за 46 с після `stop: no-progress` о 06:13:58); чи схвалила людина зміну locked-тестів саме тут — _людина: підтвердити_ (на відміну від рядка 27 Stage 2, тут навіть постановка задачі не згадує рішення людини — лише технічне пояснення в тілі коміту) | коміти `af6426a` («0.7 + 0.3 and 0.9 + 0.1 are exactly 1 in IEEE-754, so the two premise assertions could never hold», 06:14:44 UTC), `716084f` («Two boundingBox() round-trips sampled the 300 ms crit-shake at different points, so the test failed 6 runs out of 8», 06:23:07 UTC); `tasks.md` 0.17 — «`12506f8`, plus `af6426a` (wrong float premise) and `716084f` (crit box sampling in one frame)»; `pnpm tests:locked` → `ok add-upgrades-v2: 17 locked test file(s) unchanged since 716084f` | зміна locked-тесту — саме те, від чого захищає `tests:locked`; обидва випадки об'єктивно перевірювані (арифметика, 8-кратний прогін), але дозвіл поза журналом дій — див. «Зниження» |
| 36 | 2026-09-20 | Рев'ю (`reviewer`) і правки (`ui-frontend`): tick публікується лише коли combo/golden на екрані, floating +N показує зараховану суму, null-guard у `HelperZone`, точніша перевірка версії в pre-paint-скрипті, +21 новий (не locked) тест; фінальний коміт `feat(add-upgrades-v2)` | 3 · Агент | два сабагент-виклики підряд без питань людині під час кроку (8,8 хв рев'ю + 3,4 хв правки); фінальні перевірки прогнав головний агент перед комітом | сесія `adf32e82`, `.agent-log/actions.jsonl`: `Agent` 06:23:21→06:32:11 (527 335 мс), `Agent` 06:46:31→06:49:56 (203 134 мс); коміт `16262a9` (25 файлів, +1668/−221, зокрема 4 нові тест-файли `GoldenButton.test.tsx`, `MainButton.upgrades.test.tsx`, `game-store.runtime.test.ts`, `game-store.upgrades.test.ts` — 21 тест); `tasks.md` 5.1 — `pnpm check` exit 0, `Tests 384 passed (384)` у 25 файлах, `ok add-upgrades-v2: 17 locked test file(s) unchanged since 716084f`; 5.2 — `pnpm test:e2e` exit 0, 113 passed (crit e2e падав 6 з 8 прогонів до `716084f`, тепер 8 з 8 з `--repeat-each=8`); 5.3 — `pnpm test:mutation` exit 0, 95.26 % (1103 killed, 2 timeout, 51 survived, 4 no coverage) | агент сам довів правки до зелених доказів (`pnpm check`, `pnpm test:e2e`, `pnpm test:mutation`) перед комітом — локальний ризик (компоненти й тести, не харнес) |
| 37 | 2026-09-20 | Візуальна перевірка (5.4, обидві теми, обидва режими руху) і рішення прибрати Stage 4 з плану проєкту — Dopamine Clicker закінчується на Stage 3 | 1 · Асистент (5.4 і скорочення плану — рішення людини) | людина зробила візуальний огляд і дала OK у чаті — за словами постановника, в `.agent-log` відповіді немає (`AskUserQuestion` за весь Stage 3 не зафіксовано); людина вирішила закінчити проєкт на Stage 3, а не переходити до Stage 4 | коміт `f577bb7` (`docs/project-brief.md`: «~~4. Content: remaining skins, sound packs and decor.~~ — dropped on 2026-09-20; the project ends with stage 3. Stages 1-2 are implemented and archived»); `tasks.md` 5.4 `[x]` | масштаб проєкту й візуальну якість не перевіряє жоден тест — рішення лише за людиною |
| 38 | 2026-09-20 | Архівування `add-upgrades-v2` і синхронізація основних спек (`openspec/specs/*`) | 1 · Асистент | агент переніс папку зміни в `archive/2026-09-20-add-upgrades-v2` і об'єднав 9 spec-дельт у `openspec/specs/*`, закомітив без окремого `AskUserQuestion` у цій сесії (на відміну від Stage 1 рядка 21) | коміт `ab63aab` (25 файлів, +1177/−195: `click-upgrades`, `clicker-core`, `combo`, `crit`, `game-persistence`, `golden-button`, `helpers`, `localization`, `page-decor`, `reduced-motion`, `shop`) | перенесення спек у «джерело правди» — механічний, вже узгоджений крок (як у рядках 21, 31) |
| 39 | 2026-09-25 | Stage 4 (`add-content-v3`): D23 (11 реальних відео, кадр 384×216, без ліміту трьох, кадри не накладаються), red-коміт, зелена реалізація після того, як цикл помер на старті | 1 · Асистент (зміст і red-коміт — людина); виконання green — агент | людина дала каталог id, попросила більші кадри без ліміту трьох, потім явно наказала провести це через OpenSpec з red-комітом і щоб відео не накладались, і сказала довести проєкт до кінця | red `c862b01`; цикл `.agent-log/loops/2026-09-25T14-42-19-432Z-add-content-v3.jsonl` має лише `phase: start` (pid 48043 мертвий, `.running` лишився); green зроблено в цій сесії, не циклом; `pnpm check` exit 0, 521 passed, `ok add-content-v3: 21 locked test file(s) unchanged since c862b01`; `pnpm test:e2e` exit 0, 151 passed; коміт `d14d493` | людина вирішила каталог і процес; агент лише виконав green після мертвого циклу. 5.3 (`pnpm test:mutation`) і 5.4 (візуальна перевірка обох тем) лишаються за людиною; архів не робився |
| 40 | 2026-09-25 | D24: прибрати Найкращі кліпи, NFL і Камін; додати музику Експедиції (найдешевша, 2 500 / 1 500, `wTRRKguilmY`), трейлер (`2VaLOc1FpSo`, 25 000) і бій (`nLdRv_N3y00`, 30 000) | 1 · Асистент | людина дала три URL, сказала прибрати три відео і провести зміну через весь процес (спека, red-коміт, green) | red `4614af7` і `0b98a53` (e2e на 1 499 / 4 500 кліків); обидва post-commit цикли померли до перевірки (pid 71226 і 72930); green у цій сесії; `pnpm check` exit 0, 521 passed, `ok add-content-v3` since `0b98a53`; `pnpm test:e2e` exit 0, 151 passed; коміт `5cfc613` | людина вирішила склад каталогу; агент оформив D24 і виконав green після мертвих циклів |

---

## Зміни рівня

### Підвищення

> Рядок 8–9 — green-фаза піднята до **3 · Агент**: `pnpm agent:loop` працює без нагляду і сам
> доводить до зеленого. Підняв не тому, що «агент добре поводився», а тому, що зʼявився дешевий детектор:
> `pnpm check` + `pnpm test:e2e` на кожній ітерації, `tests:locked` не дає послабити тести, ліміт
> 5 ітерацій і стоп на відсутності прогресу. Агент при цьому не комітить — `feat(<change>)` робить людина.
> Рівень підтвердиться лише після першого реального прогону.

> **Рядок 14 — підтверджено.** Перший реальний прогін `add-foundation` (2026-09-19, автостарт hook'ом
> після `338dd80`) пройшов зелено за 1 ітерацію: `.agent-log/loops/2026-09-19T11-06-38-900Z-add-foundation.jsonl`
> показує `check ✘` → 1 ітерація агента (795 с) → `check ✔` (106/106, e2e 28/28) → `stop: green`. Цикл сам
> не зупинив себе жодного разу від помилки — детектор (`pnpm check` + `pnpm test:e2e` + `tests:locked` + ліміт
> ітерацій) не знадобився в дії, окрім `pnpm test:mutation`, який цикл сам відмовився запускати (не в
> allow-list) — див. рядок 15.

> Рядки 12–13, 16, 18 — сабагенти (`spec-writer`, паралельні `game-logic`/`i18n`/`e2e-qa`, `reviewer`,
> `ui-frontend`) під час Stage 1 самі доводили свою частину до доказів (тести, `pnpm check`) і передавали
> результат через `SubagentHandback`, а не через питання людині на кожен крок — фактичний рівень цих
> кроків **4 · Агенти**, хоча базовий рівень проєкту лишається 1. Записано в рядках 12, 13, 16, 18; людина
> зводила підсумок лише комітами (`338dd80`, `2fc7d33`).

> _Stage 2 — дані, не рішення (рядок 26):_ другий реальний прогін циклу (`add-shop-v1`) теж зелений, за
> 2 ітерації. Але детектор спрацював не повністю: `tests:locked` захищає лише файли тестів, і цикл
> «задовольнив» суперечливий locked-тест зміною коду (`t()` конвертує `'` → U+2019). Це спіймав лише
> рев'ю (рядок 27). Рівень 3 для циклу тримається, доки після нього обовʼязково йде рев'ю — записати,
> чи вважати рев'ю частиною детектора.

### Зниження

> _Кандидат (вирішити):_ `AGENTS.md` фіксує рівень 1 і «питати перед додаванням залежності», але
> `.agent-log` показує `mode: auto` у всіх сесіях сетапу, і агент сам виконав `pnpm add -D` (`@types/node`,
> `@fission-ai/openspec`, `@playwright/test`). Правило і фактичний рівень розійшлися. Записати тут, що
> вирішено: повернути `pnpm add` під ручне підтвердження (не auto) — або явно підняти рівень для сетапу з причиною.

> _Кандидат (вирішити) — рядок 17, Stage 1:_ `pnpm add -D vitest@^4` (пін версії, зміна `package.json` /
> `pnpm-lock.yaml`) — теж «Постійні межі» → рівень 1, теж `mode: auto`, і в `.agent-log` цієї сесії
> (`adf32e82`) немає жодного `AskUserQuestion`. Постановник задачі стверджує, що людина погодила пін
> версії до кроку `pnpm add`, але це не підтверджено записом дій — лише повідомленням поза інструментами
> (можливо, чат, не toolcall). Той самий патерн, що й вище: записати або що узгоджено вважати усне/чатове
> схвалення достатнім доказом для рівня 1 (і тоді документувати це явно як спосіб фіксації), або що надалі
> дозвіл на `pnpm add` має лишати слід у `.agent-log` (наприклад, через `AskUserQuestion`).

> _Кандидат (вирішити) — рядки 27–28, Stage 2:_ той самий патерн ще двічі. (1) `eslint.config.mjs` +
> `vitest.config.mts` (коміт `7fad809`) — «Постійні межі», рівень 1, але сесія `adf32e82` у `mode: auto`,
> і між 15:17 і 16:04 немає жодного `AskUserQuestion`; схвалення людини відоме лише з постановки задачі.
> (2) Зміна locked-тесту (`95e83ce`) — теж зі слів постановника, без сліду в журналі дій. Вирішити те саме
> питання, що й вище: чатове схвалення — достатній доказ, чи дозвіл на ask-first файли й locked-тести
> має лишати слід (`AskUserQuestion`).

> _Кандидат (вирішити) — рядок 35, Stage 3:_ той самий патерн втретє, тепер без жодного слова
> постановника про рішення людини. Два follow-up red-коміти (`af6426a`, `716084f`) правлять locked-тести
> `add-upgrades-v2` (хибна IEEE-754 передумова; двократний `boundingBox()` у різні кадри), обидва —
> об'єктивно перевірювані баги самого тесту, не суб'єктивний вибір. Але `AskUserQuestion` за весь Stage 3
> не зафіксовано жодного разу (`pnpm agent:log`), і жоден інший toolcall не показує рішення людини щодо
> саме цих двох комітів. Вирішити: чи «об'єктивно доведений баг тесту» (математика, 8-кратний прогін)
> достатня підстава для агента самому виправити locked-тест без окремого підтвердження людини, чи locked-
> тест завжди вимагає сліду людини в журналі незалежно від того, наскільки очевидна помилка.

> _Кандидат (вирішити) — рядок 24:_ сабагент `i18n` на рівні 4 змінив файл поза своєю зоною
> (`lib/i18n/index.ts`) і лишив `.bak`. Відкотив головний агент, людина не знадобилась — але межу зони
> сабагента зараз тримає лише текст `tasks.md`. Вирішити: обмежити сабагентам запис (hook / `tools`
> у `.claude/agents/*.md`) чи лишити як є і покладатися на перевірку головним агентом.

### Ескалація, якої я свідомо не зробив

> _Відкрите питання:_ `AGENTS.md` фіксує рівень 1, а в `.claude/agents/` є 7 сабагентів під паралельну
> роботу (рівень 4). Вирішити й записати тут: або явно підняти рівень для стадій із сабагентами (з причиною),
> або свідомо лишитися на 1.

> _Stage 1 (2026-09-19) — дані, а не рішення:_ на Stage 1 сабагенти фактично працювали паралельно і
> послідовно на рівні 4 (рядки 12, 13, 16, 18) без питання людини на кожен крок — сама здатність уже
> використовується, лише не названа офіційно рівнем проєкту. Питання відкрите досі: чи писати в
> `AGENTS.md` «Stage-и з OpenSpec-red→green йдуть на рівні 4», чи лишити 1 і трактувати кожен випадок
> як відхилення, яке фіксується тут заднім числом.

---

## Постійні межі

Незалежно від рівня решти роботи, **завжди рівень 1**:

- зміна харнесу (`.claude/settings.json`, hooks, `AGENTS.md`, `.claude/agents/`);
- зміна залежностей і конфігів збірки (`package.json`, `next.config.ts`, `tsconfig.json`, `eslint.config.mjs`);
- `.mcp.json`, `.env*`, ключі й секрети (hook `protect-env` блокує `.env*`);
- `git push`.

---

## Що агент запропонував і що з цього не виконано

Вивід `pnpm agent:log` станом на 2026-09-20 (після комітів `16262a9` / `f577bb7` / `ab63aab` Stage 3,
включно з роботою `autonomy-logger` над цим журналом — звідси й частина рядків `Bash`/`Read` нижче):

```
Agent actions: 1490 executed, 26 proposed but not executed, 26 failed — 13 session(s), 2026-09-17T09:27:40.879Z .. 2026-09-20T10:31:34.281Z
┌─────────┬────────────────────┬──────────┬──────────┬─────────┬────────┬──────────┬───────┐
│ (index) │ tool               │ proposed │ executed │ blocked │ failed │ time (s) │ files │
├─────────┼────────────────────┼──────────┼──────────┼─────────┼────────┼──────────┼───────┤
│ 0       │ 'Bash'             │ 823      │ 801      │ 23      │ 22     │ 3296.9   │ 0     │
│ 1       │ 'Read'             │ 266      │ 266      │ 0       │ 2      │ 7        │ 120   │
│ 2       │ 'Edit'             │ 228      │ 228      │ 0       │ 0      │ 0.6      │ 42    │
│ 3       │ 'Write'            │ 132      │ 132      │ 0       │ 0      │ 0.5      │ 107   │
│ 4       │ 'Agent'            │ 28       │ 26       │ 2       │ 2      │ 9007.4   │ 0     │
│ 5       │ 'SubagentHandback' │ 23       │ 23       │ 0       │ 0      │ 0        │ 0     │
│ 6       │ 'SendMessage'      │ 3        │ 3        │ 0       │ 0      │ 0        │ 0     │
│ 7       │ 'Grep'             │ 3        │ 3        │ 0       │ 0      │ 0.7      │ 0     │
│ 8       │ 'AskUserQuestion'  │ 3        │ 2        │ 1       │ 0      │ 0        │ 0     │
│ 9       │ 'ToolSearch'       │ 2        │ 2        │ 0       │ 0      │ 0        │ 0     │
│ 10      │ 'Monitor'          │ 2        │ 2        │ 0       │ 0      │ 0        │ 0     │
│ 11      │ 'Glob'             │ 1        │ 1        │ 0       │ 0      │ 0.6      │ 0     │
│ 12      │ 'Skill'            │ 1        │ 1        │ 0       │ 0      │ 0        │ 0     │
```

`Agent` тепер має 2 заблокованих рядки (`blocked`, замість 0 раніше): один — мовчазна втрата
red-фазового сабагента `game-logic` о 2026-09-20T05:26:51Z (рядок 33), інший — дублює власний виклик
`autonomy-logger` у поточній сесії (нерелевантний для Stage 3). `AskUserQuestion` за весь Stage 3
не зросло (3 proposed / 2 executed / 1 blocked — той самий рахунок, що й на 2026-09-19): жодного питання
людині за весь Stage 3 в журналі дій немає.

Випадки, коли агент запропонував щось неправильне або людина змінила курс:

> **2026-09-19 · Ключ Context7.** Людина додала ключ, але MCP його не бачив
> (`Missing environment variables: CONTEXT7_API_KEY`): ключа не було в жодному профілі shell, а `.mcp.json`
> читає лише змінні середовища, не `.env`. Агент перевірив профілі (без виводу значення), знайшов причину;
> людина запустила скрипт → `context7 ✔ Connected`. Після налаштування людина вирішила прибрати скрипт
> із репо — агент пропонував лишити.

> **2026-09-19 · Unit-тест циклу з фейковим агентом.** Агент за Definition of Done написав тест для
> `agent-loop.mjs` із підставним агентом (щоб `pnpm check` не витрачав токени). Людина не погодилась
> («навіщо тут щось фейкове») — тест і тестові env-змінні видалено. Наслідок: цикл без автоматичного
> тесту, що відходить від Definition of Done; доказом буде перший реальний прогін.

> **2026-09-19 · Автозапуск hook'а.** Агент спробував сам увімкнути `core.hooksPath` — заблоковано
> класифікатором auto mode. Людина вирішила вмикати сама; `prepare`-скрипт (тихе ввімкнення через
> `pnpm install`) свідомо не додано.

> **2026-09-19 · Stryker.** Перший запуск упав на symlink'ах `.claude/skills/`, другий — на пошуку
> плагіна під pnpm; залишки sandbox зламали `pnpm lint` (49 помилок). Виправлено конфігом
> (`ignorePatterns`, явний `plugins`, `tempDirName` у `node_modules/`), `eslint.config.mjs` не чіпали.

> **2026-09-19 · Stage 1, мутаційне тестування в циклі.** `pnpm agent:loop add-foundation` мав завдання
> 1.7/5.3 «Run `pnpm test:mutation`», але команда не в allow-list non-interactive циклу — денай, цикл
> лишив пункти невідміченими й написав про це в звіті (`.agent-log/loops/latest.out`). Мутаційне
> тестування довелося запускати вручну в основній сесії вже після green-фази (рядки 15–17).

> **2026-09-19 · Stage 1, `@stryker-mutator/vitest-runner` під vitest 5.** Рев'ю виявило, що скор
> мутацій — 14 % (`pnpm test:mutation` 11:30:53–11:30:58), хоча кілерабельні мутанти показані як
> survivors — інструмент, не тести, був зламаний. Виправлено пінком `vitest` до `^4.1.11`
> (комміт `6e2c3f8`) — скор став 94.56 %. Залежність змінена в `mode: auto`, без зафіксованого в
> `.agent-log` `AskUserQuestion`; постановник задачі стверджує, що людина це схвалила, але сам факт
> схвалення в toolcall-журналі не видно — див. «Зниження».

> **2026-09-19 · Stage 1, `next start` на порту 3000.** `pnpm test:e2e` о 11:42–11:44 не міг достукатись
> до застосунку — порт 3000 займав чужий процес (PID 68433/68447/19961, `ps`/`lsof`), схожий на застарілий
> `next start` у терміналі людини. Агент лише діагностував (не намагався `kill` — не у allow-list); повторний
> прогін о 12:07 пройшов (28/28), отже порт звільнила людина між комітами `6e2c3f8` і `2fc7d33`.

> **2026-09-19 · Stage 2, `e2e-qa` впав.** Із трьох паралельних сабагентів red-фази `e2e-qa` завершився
> `PostToolUseFailure` (`exit: error`, 13:09:04, через 391 с). Перезапуск 14:48:18–15:13:24. Причина (ліміт
> сесії Sonnet) і перезапуск на Opus — зі слів постановника; модель у `.agent-log` не пишеться.

> **2026-09-19 · Stage 2, `i18n` поза зоною.** Сабагент `i18n` правив `lib/i18n/index.ts` (no-op `sed -i.bak`,
> потім перезапис через `cat >`) і лишив `lib/i18n/index.ts.bak`. Головний агент порівняв із `HEAD` і
> відкотив: `git restore lib/i18n/index.ts && rm lib/i18n/index.ts.bak` (14:48:05) — рядок 24.

> **2026-09-19 · Stage 2, обхід locked-тесту в циклі.** `pnpm agent:loop add-shop-v1` (ітерація 2) зробив
> тест зеленим, додавши в `t()` конверсію `'` → U+2019 замість того, щоб повідомити про суперечність тесту
> і D18. Рев'юер це позначив; людина (зі слів постановника) не прийняла обхід і обрала follow-up red-коміт
> `95e83ce`. Конверсію з `index.ts` прибрано — у `866f6c3` цього файлу немає (рядки 26–27).

> **2026-09-19 · Stage 2, мутації в циклі — знову.** Як і на Stage 1, цикл тричі пробував `pnpm test:mutation`
> (15:32:25/27/29, сесія `f42a8267`) — не виконано, поза allow-list. Запустив головний агент (рядок 30).

> **2026-09-19 → 09-20 · Stage 3, ліміт сесії Opus у red-фазі.** Перший запуск трьох паралельних
> сабагентів (17:25:31/35/39) провалився для всіх трьох: один — явний `PostToolUseFailure` (69 с), два
> інші повернулись за 32–45 с (замало для реального написання тестів — повторний запуск наступного дня
> зайняв 5–9 хв на ті самі ролі). Після ~12-годинної паузи другий запуск (05:26:51/57/03) — двоє
> відпрацювали штатно, третій (`game-logic`) не отримав жодного `PostToolUse` і був перезапущений окремо
> (05:40:38→05:47:17). `i18n` під час свого прогону сам відновився через `git restore
> lib/i18n/dictionaries.ts lib/i18n/i18n.test.ts` і переписав файли заново. Хто вирішив зачекати ввечері і
> дозволив ранковий перезапуск `game-logic` — поза журналом дій (рядок 33).

> **2026-09-20 · Stage 3, `pnpm agent:loop add-upgrades-v2` зупинився на `no-progress`.** Після 1-ї
> ітерації лишався 1 тест, що не проходив; 2-га ітерація нічого не змінила (той самий результат), і цикл
> сам зупинився, не намагаючись обійти тест. Причиною виявився не пропущений функціонал, а два хибні
> locked-тести (хибна IEEE-754 передумова, вимірювання crit-ефекту в різні кадри) — виправлені окремими
> follow-up red-комітами `af6426a` і `716084f` без зафіксованого в журналі підтвердження людини (рядок 35).

> _Додавай сюди наступні випадки одразу, коли вони стаються._
