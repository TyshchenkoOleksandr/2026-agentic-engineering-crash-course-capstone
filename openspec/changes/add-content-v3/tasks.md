# Tasks: add-content-v3

Owners in brackets: [game-logic], [i18n], [ui-frontend], [fx-animations], [e2e-qa], [human].
Contract: `lib/game/types.ts` (do not edit; propose changes in your report). Design: `design.md`
(notation `FRESH`, `S({...})` with merged `helpers` / `levels`, `S3({...})`, `S2({...})`, `L0`,
`ST0`, `T0`, `TR({...})`, `TF(...)`, `A({...})`, `seq(...)`, `V2(...)`, `V3(...)`, `V4(...)`,
`RAW_V1(...)`, "the embed host is stubbed" is defined there).

All decisions D1–D22 were **confirmed by the human on 2026-09-20** (task 0.1): D1–D11 and D13–D22 as
proposed, **D12 changed** — achievements and their counters live in their own storage key
`dopamine-clicker:trophies`, never in the game save, so reset keeps clearing
`dopamine-clicker:save` (key `null` afterwards) and every Stage 1–3 reset behaviour, copy and e2e
expectation stays unchanged. D11 was narrowed to match: the save still bumps to v4, but only for
`videos`. The `reset.body` copy change is dropped (160 keys, no changed value).

**One item is still pending and only blocks task 5.4**: the ten real video ids of D3. Until the
human pastes them into `lib/game/videos.ts`, the shipped `PLACEHLDR01`…`PLACEHLDR10` end in the
tested `offline` placeholder. Nothing else waits on a human decision, so the agent loop can run.

## 0. Red tests

- [x] 0.1 [human] Confirm or change every DECISION in `design.md` (D1–D22). — confirmed 2026-09-20: D1–D11, D13–D22 as proposed (incl. D1 reviving Stage 4, D2 reversing the no-third-party-video non-goal, D5 `MAX_ACTIVE_VIDEOS = 3`, D6 no `sandbox`, D10 cosmetic-only achievements, D14 no secret achievements); **D12 changed** to a separate `dopamine-clicker:trophies` key, which narrowed D11 (v4 adds only `videos`), removed the `reset.body` copy change from D19 and shrank the D20 test-update list. `design.md`, every spec delta and `lib/game/types.ts` were updated accordingly and `pnpm exec openspec validate add-content-v3 --strict` was re-run. Real video ids (D3) still to come from the human.
- [x] 0.2 [game-logic] Create stubs `lib/game/videos.ts`, `lib/game/achievements.ts`, `lib/game/trophies.ts` and `lib/ui/contrast.ts` exporting every name of design D5/D6/D10/D12/D13/D18 typed with the signatures from `lib/game/types.ts`; data constants (`VIDEO_DECOR`, `VIDEO_SIZE`, `MAX_ACTIVE_VIDEOS`, `VIDEO_LOAD_TIMEOUT_MS`, `VIDEO_EMBED_HOST`, `ACHIEVEMENTS`, `ACHIEVEMENT_TOAST_MS`, `ACHIEVEMENT_TOAST_GAP_MS`, `TROPHIES_KEY`, `TROPHIES_BACKUP_KEY`, `CURRENT_TROPHIES_VERSION`) hold their real values; every function body is `throw new Error("not implemented")`. `trophies.ts` must not import from `save.ts` and the game modules must not import from `trophies.ts` (no cycles, design D10). Verify `pnpm typecheck` and `pnpm lint` exit 0. — done: `lib/game/videos.ts` (VIDEO_DECOR with the ten `PLACEHLDR..` ids, VIDEO_SIZE, MAX_ACTIVE_VIDEOS, VIDEO_LOAD_TIMEOUT_MS, VIDEO_EMBED_HOST), `lib/game/achievements.ts` (the 30 definitions, ACHIEVEMENT_TOAST_MS / _GAP_MS), `lib/game/trophies.ts` (TROPHIES_KEY, TROPHIES_BACKUP_KEY, CURRENT_TROPHIES_VERSION; imports only `./types`), `lib/ui/contrast.ts`. Every function body is `throw new Error("not implemented")`. `pnpm typecheck` exit 0, `pnpm lint` exit 0.
- [x] 0.3 [game-logic] Make the existing modules compile against the v4 contract with the smallest possible change: `lib/game/shop.ts` → 29-item `SHOP_CATALOG` with the exact video rows, `isOwned` / `buyItem` switches cover the `video-decor` kind (stub branches throw); `lib/game/state.ts` → `createInitialState` returns the v4 `FRESH` literal (ten keys, no achievements, no stats); `lib/game/save.ts` → `CURRENT_SAVE_VERSION = 4`, `migrateV3ToV4` stub (throws), `MIGRATIONS = { 1: migrateV1ToV2, 2: migrateV2ToV3, 3: migrateV3ToV4 }`, `serializeGame` / `validateGameState` typed for v4 (stub bodies throw); fix `components/` type errors with fixture updates and minimal casts only (no behaviour). Verify `pnpm typecheck` and `pnpm lint` exit 0. — done: `shop.ts` builds the ten `video-decor` rows from `VIDEO_DECOR` + a local `VIDEO_PRICES` table (29 items) and both switches throw for `video-decor`; `state.ts` returns the ten-key v4 literal; `save.ts` has `CURRENT_SAVE_VERSION = 4`, a throwing `migrateV3ToV4`, `MIGRATIONS = { 1, 2, 3 }` and throwing `serializeGame` / `validateGameState` (the v3 normalizers stay in the file for task 1.8 and surface as 7 eslint *warnings*; exit code still 0); `components/ShopBox.tsx` only gains the `video: "shop.category.video"` label entry (no heading rendered — that is task 3.6). `pnpm typecheck` exit 0, `pnpm lint` exit 0.
- [x] 0.4 [game-logic] Write `lib/game/videos.test.ts` covering every `[unit]` scenario in `specs/video-decor/spec.md`; verify it fails with "not implemented" (the catalog / constant scenarios may pass). — done: 10 tests (the 9 spec scenarios + a no-mutation check on `getActiveVideos`), 9 failed with "not implemented"; "Catalog ids, order and constants" passes.
- [x] 0.5 [game-logic] Write `lib/game/achievements.test.ts` covering every `[unit]` scenario in `specs/achievements/spec.md` (catalog, stats from state + trophy counters, fixed-point evaluation, sticky unlocks after a wipe, toast queue, "the economy does not know about trophies") with local `A({...})`, `T0` / `TR({...})` helpers and `deepFreeze`; verify it fails with "not implemented". — done: 28 tests covering all 28 `[unit]` scenarios of the delta (incl. "The game state has no trophy fields", both meta scenarios and the post-reset metric sourcing of "Counters survive a wiped state" / "Unlocks are sticky"); 26 failed with "not implemented", the two catalog scenarios pass.
- [x] 0.6 [game-logic] Write `lib/game/trophies.test.ts` covering every `[unit]` scenario of the ADDED requirements "Trophy file format and validation" and "Loading trophies with fallback" in `specs/game-persistence/spec.md` (in-memory `KeyValueStorage` fake, throwing-storage fake); verify it fails with "not implemented". — done: 34 tests (the `[unit]` scenarios of both ADDED requirements with the `it.each` rows counted individually, plus the game-persistence "Reset trophies keep the unlocked ids" scenario and two purity checks); all 34 fail with "not implemented".
- [x] 0.7 [game-logic] Write `lib/ui/contrast.test.ts` covering every `[unit]` scenario of the requirement "Colour helpers for contrast assertions" in `specs/button-skins/spec.md`; verify it fails with "not implemented". — done: 11 tests (the 6 scenarios, with "Unparseable values" expanded into 6 `it.each` cases); all 11 fail with "not implemented".
- [x] 0.8 [game-logic] Rewrite `lib/game/shop.test.ts` to every `[unit]` scenario in `specs/shop/spec.md` (29-item catalog, video reveal boundaries, buying a video with `videoPosition`, order); verify the new ones fail on assertions / "not implemented". — done: 67 tests; the catalog / reveal-boundary / price scenarios pass against the real v4 catalog data, and the 6 that reach the `video-decor` switch branches ("Revealed items at 20 clicks", "Everything revealed late in the game", "Videos are revealed one by one", "Nothing revealed before 10", "Buy a video with a position", "Videos keep catalog order and can be owned only once") fail with "not implemented".
- [x] 0.9 [game-logic] Rewrite `lib/game/state.test.ts` to the MODIFIED `[unit]` scenarios in `specs/clicker-core/spec.md` (v4 `FRESH`, exact key list) and `lib/game/save.test.ts` to every save-related `[unit]` scenario in `specs/game-persistence/spec.md` (v4 envelope with `videos`, v4 validation, `MIGRATIONS` with three entries, `migrateV3ToV4`, `S3` / `V3` / `V4` helpers); update only the `S()` fixture in `lib/game/{skins,helpers,click-value,press,decor}.test.ts`; verify failures are assertions / "not implemented", never syntax or import errors. — done: `state.test.ts` (18 tests) passes against the v4 `FRESH` incl. the exact ten-key list; `save.test.ts` has 141 tests (new `S3` / `V3` / `V4` helpers, the v4 envelope, the three video-validation scenarios, the eight v3 → v4 scenarios, `MIGRATIONS` with three entries) of which 95 fail, all through the `serializeGame` / `validateGameState` / `migrateV3ToV4` stubs (`not implemented`, or a load falling back to `corrupted` / `saveGame` returning `false`); `skins`, `helpers`, `click-value`, `press` got the `videos: []` fixture line only and still pass (`decor.test.ts` has no state fixture, so it needed none). The six fixture-only lib files run 81 passed / 81.
- [x] 0.10 [game-logic] Update fixtures in `components/{game-store,game-store.runtime,game-store.shop,game-store.upgrades,ShopBox}.test.ts(x)` to v4 states and the expected envelope to `{ version: CURRENT_SAVE_VERSION, state: S(...) }`, no behavioural change; verify `pnpm typecheck` exits 0. — done: `videos: []` added to the `S()` fixture of `game-store`, `game-store.runtime`, `game-store.shop` and `game-store.upgrades`; the envelope assertion already reads `CURRENT_SAVE_VERSION`, so it needed no edit; `ShopBox.test.tsx` builds its state from `createInitialState()` and needed no fixture change. `pnpm typecheck` exit 0. Residual red in `components/` comes only from the stubs: `game-store.test.ts` 1/5, `game-store.shop.test.ts` 1/6, `ShopBox.test.tsx` 2/3.
- [x] 0.11 [i18n] Add the 92 new keys (rows 69–160 of `specs/localization/spec.md`) to `uk` and `en` in `lib/i18n/dictionaries.ts` with value `""` (stub) and change no existing value; update `lib/i18n/i18n.test.ts` to every `[unit]` scenario in `specs/localization/spec.md` (160-key set, 29 item names / descriptions, 30 achievement names / descriptions, Stage 4 copy and interpolation, no-break spaces); verify key parity passes and the copy scenarios fail.
- [x] 0.12 [e2e-qa] Write `e2e/add-content-v3.spec.ts` with one `describe` per capability (achievements, video-decor, button-skins, page-decor, shop, game-persistence, reduced-motion) and one `test` per `[e2e]` scenario marked "new" or living in the new capabilities, selecting only by `data-testid`, role, `aria-*` / `data-*` attributes. Local helpers: `S()` (v4, merged `helpers` / `levels`), `S3()`, `TR()` / `TF()` for the trophy file, `seedStorage` (sessionStorage guard from `e2e/add-foundation.spec.ts`), `seedV3()`, `seedV4()`, `seedTrophies()`, `readTrophies(page)`, `fixRandom(page, r)`, `pauseClock(page)`, `strictlyOverlap`, `animationNames(locator)`, `boxOf(locator)`, and `stubEmbeds(page)` installing the route of design D9 in `beforeEach` **before the first navigation** plus a call counter; colour assertions import `parseCssColor` / `compositeOver` / `contrastRatio` / `lastShadowColor` from `@/lib/ui/contrast`. No test may contact a real third-party host. — done 2026-09-20: audited every `[e2e]` scenario of the nine `specs/*/spec.md` files (78 total across all capabilities; 38 marked "new"/living in a new capability, the other 40 confirmed already present unchanged in `add-foundation.spec.ts` / `add-shop-v1.spec.ts` / `add-upgrades-v2.spec.ts` by name-grep). `e2e/add-content-v3.spec.ts` written with exactly those 38 tests across the 7 required describes (achievements 9, video-decor 10, button-skins 3, page-decor 4, shop 3, game-persistence 4, reduced-motion 5); `npx playwright test e2e/add-content-v3.spec.ts --list` reports `Total: 38 tests in 1 file`. `test.beforeEach` installs the D9 route stub (fulfil, with a request counter `embedStub`) before every test's first `page.goto`; offline scenarios override it with `abortEmbeds`; "No unstubbed external request is made" is the explicit guard test. `pnpm lint` exit 0 (0 errors; the 7 remaining warnings are pre-existing in `lib/game/save.ts`, untouched by this task). Could not exercise: the file imports the still-stubbed `lib/ui/contrast.ts` and `lib/game/videos.ts` (`buildEmbedUrl`/`getVideoEntry`), so every test that calls them at collection/run time is expected to fail red on "not implemented", per instructions — not loosened.
- [x] 0.13 [e2e-qa] Update only the expected save envelopes of the Stage 1–3 e2e specs (design D20): `e2e/add-foundation.spec.ts` ("Corrupted save in the browser", "Reload keeps balance"), `e2e/add-shop-v1.spec.ts` ("Buying a skin through the UI", "Stage 1 player continues in Stage 2", "No catch-up for closed time" — now 10 state keys) and `e2e/add-upgrades-v2.spec.ts` ("Stage 2 player continues in Stage 3"). Change nothing else; confirm that "All items at 750 clicks", "Reset wipes purchases", "Confirm wipes game but keeps theme and language" and "Reset clears Stage 3 progress and runtime" still pass unchanged (no video is revealed before 3 000 clicks and the save key is still `null` after a reset). — done 2026-09-20: `git diff --stat` shows only `add-foundation.spec.ts` (+6/-2), `add-shop-v1.spec.ts` (+17/-3, includes a new local `S4()` fixture helper) and `add-upgrades-v2.spec.ts` (+13/-2, same `S4()` pattern); `git diff` confirms the only behavioural edits are `version: 3` → `4` plus a `videos: []` key in the five named scenarios (the "No catch-up for closed time" key-list scenario is the fifth, inside "Buying a skin..."/"Stage 1 player continues in Stage 2"'s describe). Grepped "All items at 750 clicks", "Reset wipes purchases", "Confirm wipes game but keeps theme and language" and "Reset clears Stage 3 progress and runtime" — none appear in the diff, confirmed untouched.
- [ ] 0.14 [game-logic] Run `pnpm typecheck`, `pnpm lint` (both exit 0), `pnpm test` and `pnpm test:e2e` (both fail only on assertions / "not implemented" / missing elements); record exit codes and pass / fail counts in this task. — Vitest half done (2026-09-20, game-logic): `pnpm typecheck` exit 0; `pnpm lint` exit 0 (7 `no-unused-vars` warnings in `lib/game/save.ts`, the v3 normalizers kept for task 1.8); `pnpm test` exit 1 with **200 failed / 306 passed (506 tests, 29 files, 10 files red)**. Per failing file: `lib/game/save.test.ts` 95/141, `lib/game/trophies.test.ts` 34/34, `lib/game/achievements.test.ts` 26/28, `lib/i18n/i18n.test.ts` 15/26 (tasks 0.11 / 2.1, i18n owner), `lib/ui/contrast.test.ts` 11/11, `lib/game/videos.test.ts` 9/10, `lib/game/shop.test.ts` 6/67, `components/ShopBox.test.tsx` 2/3, `components/game-store.test.ts` 1/5, `components/game-store.shop.test.ts` 1/6. Every failure is `Error: not implemented` or an assertion that follows directly from a stub (a swallowed stub throw makes `saveGame` return `false` or a load fall back to `corrupted`); no syntax, import or type errors. `pnpm test:e2e` still to be run once tasks 0.12 / 0.13 land.
- [ ] 0.15 [human] Commit stubs + tests as `test(add-content-v3): add failing tests`; verify `git log --oneline --grep "^test(add-content-v3)"` shows it and `pnpm tests:locked` prints `ok add-content-v3`.

## 1. Game logic (lib/game, lib/ui) [game-logic]

- [x] 1.1 Implement `getVideoEntry`, `buildEmbedUrl` (exact parameter order of design D6) and `getActiveVideos` in `lib/game/videos.ts`; verify `pnpm test lib/game/videos.test.ts` passes. — done: `getVideoEntry` throws for an unknown id, `buildEmbedUrl` builds the D6 parameter list with `&start=<n>` appended only for an integer > 0, `getActiveVideos` filters `position !== null` and slices to `MAX_ACTIVE_VIDEOS` without mutating. `pnpm test lib/game/videos.test.ts` 10/10 pass.
- [x] 1.2 Implement `getAchievement`, `isAchievementUnlocked`, `getAchievementProgress` and `getAchievementStats(state, trophies)` in `lib/game/achievements.ts` (eleven state-derived metrics + the four trophy counters, `achievementsUnlocked` 0, no mutation); verify the catalog / stats tests pass. — done; `purchases` = skins + decor + videos + upgrades + helper counts + Σ levels.
- [x] 1.3 Implement `evaluateAchievements` in the same module (fixed-point passes bounded by `ACHIEVEMENTS.length`, sticky unlocks, unknown ids dropped, catalog order, no mutation); verify the evaluation tests pass. — done: passes stop as soon as one adds nothing, `achievementsUnlocked` is recomputed per pass from the growing set.
- [x] 1.4 Implement `createToastQueue`, `enqueueToasts` and `advanceToastQueue` (clamp to `MAX_TICK_MS`, same-object return when idle or elapsed 0, no leftover carry); verify `pnpm test lib/game/achievements.test.ts` passes. — done: `pnpm test lib/game/achievements.test.ts` 28/28 pass.
- [x] 1.5 Implement `lib/game/trophies.ts`: `createInitialTrophies`, `serializeTrophies`, `validateTrophies` (normalization into `ACHIEVEMENTS` order, unknown keys dropped), `parseTrophies` (version ≠ 1 ⇒ `corrupted`), `loadTrophies` (backup to `:trophies:bad`, never throws), `saveTrophies` (`false` on failure), `clearTrophies`; verify `pnpm test lib/game/trophies.test.ts` passes. — done: the module imports only `./achievements` and `./types` (never `save.ts`); `pnpm test lib/game/trophies.test.ts` 34/34 pass.
- [x] 1.6 Implement `parseCssColor`, `compositeOver`, `relativeLuminance`, `contrastRatio` and `lastShadowColor` in `lib/ui/contrast.ts` (paren-aware splitting of the `box-shadow` layers); verify `pnpm test lib/ui/contrast.test.ts` passes. — done: 11/11 pass; luminance is rounded to 6 decimals so pure white is exactly 1.
- [x] 1.7 Implement in `lib/game/shop.ts` the `video-decor` branches: `isOwned` via `state.videos`, `buyItem` adding `{ id, position: options.videoPosition ?? null }` in catalog order; verify `pnpm test lib/game/shop.test.ts` passes. — done: 67/67 pass.
- [x] 1.8 Implement v4 `serializeGame` (exactly the ten schema keys), v4 `validateGameState` (the `videos` rules and normalization of design D11; a stray `achievements` / `stats` key is dropped) and `migrateV3ToV4` in `lib/game/save.ts`; verify `pnpm test lib/game/save.test.ts lib/game/state.test.ts` passes. — done: `normalizeDecor` generalized to `normalizePlaced(value, knownIds)` and reused for `videos`; `pnpm test lib/game` 408/408 pass (15 files).
- [ ] 1.9 Run `pnpm test:mutation`; verify the score is ≥ 70 % for `lib/**`, adding new (never editing locked) tests for surviving mutants; report the score. — **not run**: `pnpm test:mutation` is not on the agent loop's allow-list, so the run was refused. It is still open and overlaps with the human task 5.3. The Stage 4 `lib/` modules are covered by the locked red tests (`videos` 10, `achievements` 28, `trophies` 34, `contrast` 11, `shop` 67, `save` 141 assertions), so the expectation is that the threshold holds, but that is a claim, not evidence.

## 2. Dictionaries (lib/i18n) [i18n]

- [x] 2.1 Fill the 92 new keys in `uk` and `en` with the exact copy of `specs/localization/spec.md` (U+2019 apostrophes, U+00A0 before thousands in Ukrainian numbers) and leave every existing value untouched; verify `pnpm test lib/i18n` passes. — copy filled from the spec table (rows 69–160), no existing value changed; the five Ukrainian thousands use ` ` escapes so the code point is unambiguous. `pnpm test lib/i18n` goes from 15 failed / 11 passed to **9 failed / 17 passed** and the remaining 9 cannot be satisfied — see the blocker note under section 2 below.

**BLOCKER (human decision, task 2.1 / 0.11): `lib/i18n/i18n.test.ts` of the red commit lost every
non-ASCII special character and is self-contradictory.** The file contains 0 occurrences of U+00A0
and U+2019 (`dictionaries.ts` has 3, `specs/localization/spec.md` has 9), so `const APOSTROPHE`
(line 6, comment "U+2019") is an ASCII `'` and `const NBSP` (line 8, comment "U+00A0") is an ASCII
space. Three assertions are then impossible for *any* dictionary:

1. row 68 of the file's own `COPY` table is `Золотий бонус ×7: {seconds} с`, while line 472 expects
   `t("uk", "golden.bonus", { seconds: 30 })` to be `Золатий бонус ×7: 30 с` (an `о` → `а` typo);
2. lines 446–456: the Ukrainian `item.crit.description` must be `5 % → 10 % → 15 %` with the
   flattened ASCII `NBSP` *and* must not match `/\d %/` (also ASCII) — mutually exclusive;
3. line 462: the English `item.crit.description` ("Chance for a click to count ×10: 5% → 10% → 15%")
   must not contain `" "` (ASCII space), which it necessarily does.

The other six failures are the same flattening (`М’яка тінь` vs `М'яка тінь`,
`Набери 1 000 …` vs `Набери 1 000 …`, `formatNumber(1234, "uk")`). Satisfying them would mean
rewriting existing Stage 1–3 copy and `formatNumber` to ASCII, which contradicts design D19 ("no
existing value changes"), `specs/localization/spec.md` and the tests' own names. A human has to
re-create the red commit's `lib/i18n/i18n.test.ts` with the real U+2019 / U+00A0 characters (and the
`Золотий` spelling); the implementation side of 2.1 is done and needs no further change.

## 3. Screen wiring (components/, app/, docs/) [ui-frontend]

- [x] 3.1 Extend `components/game-store.ts` per design D21: load the trophy file once with `loadTrophies` next to the save, keep it in module state, publish it in the runtime snapshot, and add one internal `syncTrophies({ counters?, silent? })` that applies counter deltas, runs `evaluateAchievements(trophies.unlocked, getAchievementStats(snapshot, next))`, enqueues `newlyUnlocked` unless `silent`, calls `saveTrophies` and publishes. Call it after `press` (raising `crits` on a crit and `maxComboLevel` to the post-press level), `buy`, `toggle`, `tick`, `catchGoldenButton` (raising `goldenCaught` — **trophy write only, the game save must not be written**) and `resetGame` (raising `resets`, evaluated against the pre-reset state, silent). The initial load evaluates with `silent: true`. Keep `resetGame`'s save behaviour exactly as today (`clearGame`, key `null`) and reset the runtime, both carries and the toast queue. Add tests in a new `components/game-store.content.test.ts`; verify `pnpm test components` passes. — done, with **two deviations from D21 that the locked tests force**: (a) the trophies and the toast queue are published in their **own** store (`subscribeTrophies` / `getTrophiesSnapshot` / `getServerTrophiesSnapshot`), not in the runtime snapshot, because `components/game-store.upgrades.test.ts` pins `getServerRuntimeSnapshot()` to exactly `{ runtime, now }` and `game-store.runtime.test.ts` pins the runtime snapshot's referential stability across ticks that unlock achievements; (b) unlocks found **without a player action** (the evaluation on load, a helper tick, the reset) are held in a `pendingToasts` list and flushed into the queue by the next player action instead of being dropped. (b) is what makes the e2e pair "A loaded save unlocks silently" (no toast after load) and "Full motion plays the Stage 4 effects" (a toast after the first click on a save whose unlocks all come from the load) both pass; dropping them, as D13 reads literally, fails the second. `pnpm test components` 64/64 pass (12 files).
- [x] 3.2 Add `components/AchievementsPanel.tsx` (button `[data-testid="achievements"]` in the top-right cluster left of the language toggle + the `<dialog>` of design D14 with count, 30 rows, `data-unlocked`, progress rows, close button, Escape) reading the trophies from the runtime snapshot; render it from `GameScreen`; verify the achievements panel e2e scenarios pass. — done (it reads the trophies from the new trophy store, see 3.1); the 6 achievements-panel e2e scenarios pass.
- [x] 3.3 Add `components/AchievementToast.tsx` (`[data-testid="achievement-toast"]`, `role="status"`, `aria-live="polite"`, text `achievements.toast`, one at a time from the published queue) and render it from `GameScreen`; verify the toast e2e scenarios pass. — done; "The first click shows one toast", "Two unlocks queue instead of stacking" and the three reduced-motion toast scenarios pass.
- [x] 3.4 Add `components/VideoDecorLayer.tsx` per design D7/D8: one root per bought video with `data-video-state`, the placeholder, the lazy `IntersectionObserver`-gated iframe with the attributes of D6, the `VIDEO_LOAD_TIMEOUT_MS` timer, the `online` listener and the reduced-motion play button; render it from `GameScreen`; verify the video-decor e2e scenarios pass. — done; 9 of the 10 video-decor scenarios pass (the 10th, "No network falls back to the placeholder", is blocked — see the blocker note at the end of section 3). The root keeps `z-index: 30`: `position: fixed` already makes it a stacking context, so the reduced-motion play button cannot rise above the shop box on its own, and "Reduced motion never autoplays a video" clicks that button while a video sits under the shop. The root still takes no pointer events, so "A video never blocks the main button" is unaffected.
- [x] 3.5 Extend `components/DecorLayer.tsx` / the reserved-rect helper with `[data-testid="achievements"]` and every placed video rect, and share the fallback dock between decor and videos (decor first, then videos, catalog order, 8 px gap); verify page-decor "Decor avoids the achievements button and placed videos" passes. — done: `reservedRects(decor, viewport, videos)` adds `achievements` and every placed video, `fallbackDockRects(decor, viewport, videos)` is shared, and `chooseVideoPosition` places a bought video with `VIDEO_SIZE`. That scenario and "Videos and decor never overlap each other" pass.
- [x] 3.6 Extend `components/ShopBox.tsx` with the fourth category heading `[data-testid="shop-category-video"]` (rendered last, only when a video is revealed) and the owned label for videos; pass `videoPosition` from `GameScreen`'s buy handler for `video-decor` items; verify the shop e2e scenarios pass. — done: `"video"` was appended to `CATEGORIES` (the heading renders only when the category has a revealed item, which the existing code already handles) and the generic owned label covers videos. All three shop scenarios pass.
- [x] 3.7 Update the pre-paint script in `components/BalanceCounter.tsx` to accept save versions 1…4; verify "Stage 3 player continues in Stage 4" and "Seeded save is shown with locale formatting" pass. — **no change needed**: the script already interpolates `CURRENT_SAVE_VERSION` (`ver>=1&&ver<=4` since task 0.3 bumped the constant). Both scenarios pass.
- [x] 3.8 Verify that `components/ResetProgress.tsx` needs **no** change (the copy and the flow are unchanged under design D12) and that the Stage 1–3 reset e2e scenarios still pass untouched; record the result in this task. — confirmed: `ResetProgress.tsx` is untouched (`git status` shows no change to it), and "Confirm wipes game but keeps theme and language" (add-foundation), "Reset wipes purchases" (add-shop-v1), "Reset clears Stage 3 progress and runtime" (add-upgrades-v2) and the Stage 4 "Reset keeps the trophies and unlocks the reset achievement" all pass.
- [x] 3.9 Update `docs/project-brief.md` (confirmed in 0.1): (a) line ~158 of the delivery plan — replace the struck-through "dropped on 2026-09-20" Stage 4 line with a live Stage 4 "Content v3" entry naming the five items of this change and stating it was revived on 2026-09-20 with a different scope (the original skins / sounds / decor list stays out of scope); (b) the non-goals — narrow "No third-party videos … no YouTube embeds" to "we ship no copyrighted footage of our own; the `video` decor category embeds third-party players the owner configures in `lib/game/videos.ts`", keeping "no real money, ads or analytics"; (c) add the video and achievement content to the Shop section (a `Video decor` table with the ten items and their prices, and a one-line note that 30 cosmetic achievements exist and are stored outside the game save). Quote the edited blocks in the task note. — done, three edits:

(a) delivery plan: "4. **Content v3:** revived on 2026-09-20 with a different scope than the
original "remaining skins, sound packs and decor" (that list stays out of scope): a recognizable
sleeping-cat SVG, a Soft shadow that is visible in dark mode, a readable Floating +N pill, ten
third-party video decor items and thirty cosmetic achievements. Stages 1-3 are implemented and
archived (`add-foundation`, `add-shop-v1`, `add-upgrades-v2`)."

(b) non-goals: "We ship no copyrighted footage of our own; every decoration we draw is our own CSS /
SVG / canvas animation. The `video` decor category (stage 4, revived on 2026-09-20) embeds
third-party players whose ids the owner configures in `lib/game/videos.ts`, on the privacy-friendly
`www.youtube-nocookie.com` host, for their personal build." ("No real money, ads or analytics"
stayed.)

(c) shop: a new "### 2b. Video decor (stage 4)" section with the ten-row price / reveal table and
the closing line "Thirty cosmetic achievements (no reward of any kind) are listed in a panel and
announced by a toast; they live outside the game save, in their own `dopamine-clicker:trophies` key,
so a progress reset keeps the trophy case."

**BLOCKER (human decision, task 0.12 / design D9): the e2e scenario "No network falls back to the
placeholder" cannot pass as written.** It makes the embed host fail with `route.abort("failed")` and
expects `data-video-state="offline"` after 5 000 ms. Chromium fires the iframe's `load` event for an
aborted subframe navigation exactly as for a fulfilled one, and the two are indistinguishable from
inside the page — measured with a throwaway probe spec: aborted →
`{"events":["load"],"href":"throws","origin":"throws","frames":0}` and resource timing
`{requestStart:0, responseStart:0, responseEnd:211, encoded:0, decoded:0, deliveryType:""}`;
fulfilled → the same values (`responseEnd:210`). No `error` event, no readable document, no
cross-origin timing. Any probe that would tell them apart (a `fetch` before mounting) costs a second
request and breaks "A placed video mounts a muted looping frame" (`embedStub.count()` must be 1) and
"Inactive videos cost no request" (exactly 3). Design D9 itself names the fix: the offline scenario
should use a route that **never fulfils** and let `VIDEO_LOAD_TIMEOUT_MS` expire on the fake clock
(or `context.setOffline(true)`), instead of `abortEmbeds`. A human has to change that one red test;
the implementation already has both paths (`navigator.onLine === false` at mount, and the 5 s
timeout that unmounts the frame).

## 4. Motion and visuals (app/globals.css, components) [fx-animations]

- [x] 4.1 Replace the sleeping-cat CSS boxes in `components/DecorLayer.tsx` with the inline SVG of design D15 (eight parts, their test ids, the viewBox and the geometry of the table) and re-point `cat-breathe`, `cat-ear-twitch` and `cat-tail-sway` at the SVG parts (`transform-box: fill-box; transform-origin` set per part so the keyframes still work on SVG nodes); verify the page-decor cat scenarios pass. — done; all four cat scenarios pass. Two deviations from the D15 geometry table, both forced by how Playwright measures an animated SVG node (its `boundingBox()` is the transformed, stroke-inclusive quad, which is much larger than `getBBox()`): the tail is drawn as `M64 54 C48 66 22 62 16 48` (stroke 7) instead of the `{ x: 6, y: 50, w: 52, h: 26 }` of the table, and `cat-tail-sway` swings −8° instead of −12°. With the table's values the rotated tail measures `x = 58` against a root at `x = 64` and fails "cat-tail inside root"; the shipped values measure `x = 66`, keep `tail.x (66) < body.x (78)` and `tail.width (76) ≥ 0.25 × body.width (22)`, so every silhouette invariant still holds.
- [x] 4.2 Add the `--skin-shadow*` tokens of design D16 to `:root` / `[data-theme="light"]` / `[data-theme="dark"]` and make `.skin-soft-shadow` read them only; verify "Soft shadow is visible in both themes" and the existing "Soft shadow changes the shadow" pass. — done, both pass. `box-shadow` also had to leave `.main-button`'s `transition` (only `transform` is transitioned now): the skin class is added at hydration, so a 150 ms shadow fade made the computed value unreadable right after load (the alpha was measured at 0.04 mid-fade). add-foundation's "Full motion has a press transition" / "Reduced motion has no press transition" still pass.
- [x] 4.3 Restyle `[data-testid="floating-number"]` as the pill of design D17 (`--fx-float-bg`, `--fx-float-fg`, `z-index: 60`, padding, radius, still `pointer-events: none`, still portalled to `document.body`); confirm `<main>` and `[data-testid="shake-layer"]` keep `z-index: auto`; verify the two new floating-number scenarios and the existing ones pass. — done: the Tailwind classes `z-30 text-accent` were dropped from the span so the `.floating-number` rule owns colour and z-index; `<main>` and the shake layer keep `z-index: auto`. The two new scenarios and add-shop-v1's "Floating number shows the earned amount" / "Floating number with Double click" pass.
- [x] 4.4 Add the keyframe `toast-in` (200 ms, translateY + opacity) and the video placeholder art; verify "Full motion plays the Stage 4 effects" passes. — done: `@keyframes toast-in` on `.achievement-toast` and a `.video-placeholder` gradient card; that scenario passes.
- [x] 4.5 Extend the `@media (prefers-reduced-motion: reduce)` and `[data-motion="reduced"]` blocks: toast `animation: none`, every cat SVG part `animation: none`, video roots and their contents `animation: none`; verify every `specs/reduced-motion/spec.md` Stage 4 scenario passes. — done: `.achievement-toast`, `.video-decor` and `.video-decor *` joined both blocks (the cat SVG parts were already covered by the existing `.decor *`). All five reduced-motion scenarios pass.

## 5. Verify and commit [human + agent loop]

- [x] 5.1 Run `pnpm check` (typecheck + lint + tests:locked + test); record exit code, test count and the `ok add-content-v3` line. — run on 2026-09-20: `pnpm typecheck` exit 0, `pnpm lint` exit 0 (no errors, no warnings), `pnpm tests:locked` prints `ok      add-content-v3: 20 locked test file(s) unchanged since 9688ed9`, `pnpm test` **506 passed / 9 failed (515 tests, 30 files)** so `pnpm check` exits 1. All nine failures are in `lib/i18n/i18n.test.ts` and are the unsatisfiable, character-mangled assertions described in the blocker under section 2; every other file is green. Re-run 2026-09-25 after D23: `pnpm check` exit 0, `pnpm tests:locked` `ok add-content-v3: 21 locked test file(s) unchanged since c862b01`, `pnpm test` 521 passed.
- [x] 5.2 Run `pnpm test:e2e`; record exit code and pass count (add-foundation + add-shop-v1 + add-upgrades-v2 + add-content-v3 + smoke). Confirm from the run that no request reached a real third-party host. — run on 2026-09-20: **149 passed / 2 failed (151 tests)**, exit 1. The two failures are the locked-test blockers: `add-content-v3` "No network falls back to the placeholder" (section 3 blocker) and `add-upgrades-v2` "Stage 3 purchases survive reload", which still asserts `version: 3` for a save written after a purchase — impossible with `CURRENT_SAVE_VERSION = 4`; design D20's update list (task 0.13) missed it, so a human has to update that one envelope assertion in the red commit. All 38 `add-content-v3` scenarios except the first one pass, including the guard "No unstubbed external request is made", which confirms every request went to `localhost` or the stubbed `www.youtube-nocookie.com` — no real third-party host was contacted. Re-run 2026-09-25: `pnpm test:e2e` exit 0, 151 passed.
- [ ] 5.3 [human] Run `pnpm test:mutation`; record the score (≥ 70 %) and explain surviving mutants in the PR.
- [ ] 5.4 [human] Paste the ten real video ids into `VIDEO_DECOR` in `lib/game/videos.ts` (D3 is the only open item), then do the visual check in both themes and both motion modes: the sleeping cat actually reads as a cat (the geometry test cannot judge that), the Soft shadow is obvious in dark mode, the Floating +N is readable over the button, the achievements panel and toast look right, and the ten videos play.
- [ ] 5.5 [human] Commit `feat(add-content-v3): cat svg, theme-aware skin fixes, video decor and achievements` after 5.1–5.4 are green, then archive the change.

## 6. Follow-up confirmed 2026-09-25 (design D23)

- [x] 6.1 [human] Red commit `test(add-content-v3): revise the video catalog, frame size and non-overlap` after the spec update. The overlap tests fail with `not implemented`. — `c862b01`. The post-commit loop exited before a check; `.agent-log/loops/2026-09-25T14-42-19-432Z-add-content-v3.jsonl` has only the start line and pid 48043 is gone.
- [x] 6.2 Implement `separatePlacedVideos` and use it for rendering and the next purchase. Do not edit the tests from 6.1.
