# Tasks: add-upgrades-v2

Owners in brackets: [game-logic], [i18n], [ui-frontend], [fx-animations], [e2e-qa], [human].
Contract: `lib/game/types.ts` (do not edit; propose changes in your report). Design: `design.md`
(notation `FRESH`, `S({...})` with merged `helpers` / `levels`, `S2({...})`, `seq(...)`, `V2(...)`,
`V3(...)`, `RAW_V1(...)`, `C(...)`, `G(...)`, `VIS(...)`, `P`, `place(P)`, `placeNull` is defined
there).

All DECISION items in `design.md` were confirmed by the human on 2026-09-19 (task 0.1): D1–D8,
D10–D12, D15, D20, D21 as proposed (incl. revealAt table and timing numbers). D9 was changed to an
exact click value with a runtime-only main-click carry. Resolved questions: test-only random hook
`globalThis.__dcRandom` (D19), shake on `[data-testid="shake-layer"]` only (D12), Ukrainian
percentages with U+00A0 (D21), whole-save rejection (D15), ~75 s fake-clock runs accepted (D19).
No open `DECISION` items remain.

## 0. Red tests

- [x] 0.1 [human] Confirm or change every DECISION listed above (design D1–D12, D15, D20, D21); if any value changes, update `design.md` and the affected spec deltas, re-run `pnpm exec openspec validate add-upgrades-v2 --strict`, and only then continue. — confirmed 2026-09-19; D9 changed to the main-click carry, open questions resolved (see above); specs and design updated, validation re-run.
- [x] 0.2 [game-logic] Create stubs `lib/game/crit.ts`, `lib/game/combo.ts`, `lib/game/golden.ts`, `lib/game/press.ts` exporting every name in design D14 typed with the signatures from `lib/game/types.ts`; constants (`CRIT_MULTIPLIER`, `CRIT_CHANCE_BY_LEVEL`, `COMBO_*`, `GOLDEN_*`) hold their real values; every function body is `throw new Error("not implemented")`. — done 2026-09-20: 4 new modules, real constants, 11 stubbed functions.
- [x] 0.3 [game-logic] Make the existing modules compile against the v3 contract with the smallest possible change: `lib/game/shop.ts` → 19-item `SHOP_CATALOG` with the exact catalog data, `getItemLevel` stub, `isOwned` / `buyItem` switches cover the new kinds (stub branches throw); `lib/game/helpers.ts` → `SPEED_UP_OF` constant, `getHelperRate` stub; `lib/game/state.ts` → `createInitialState` returns the v3 `FRESH` literal; `lib/game/save.ts` → `CURRENT_SAVE_VERSION = 3`, `migrateV2ToV3` stub (throws), `MIGRATIONS = { 1: migrateV1ToV2, 2: migrateV2ToV3 }`, `serializeGame` / `validateGameState` typed for v3 (stub bodies throw); `lib/game/click-value.ts` → `getClickModifiers(state, context?)` signature and a `creditClick` stub (throws); fix `components/` type errors with minimal casts only (no behaviour); verify `pnpm typecheck` and `pnpm lint` exit 0. — done 2026-09-20: `pnpm typecheck` exit 0, `pnpm lint` exit 0; no casts were needed in `components/` (fixtures updated in 0.12 instead).
- [x] 0.4 [game-logic] Write `lib/game/crit.test.ts` covering every `[unit]` scenario in `specs/crit/spec.md` (a `seq(...values)` fake that throws when over-called and exposes its call count); verify it fails with "not implemented" (constant scenarios may pass). — done 2026-09-20: 6 tests (1 passed = constants, 5 failed with "not implemented").
- [x] 0.5 [game-logic] Write `lib/game/combo.test.ts` covering every `[unit]` scenario in `specs/combo/spec.md`; verify it fails with "not implemented". — done 2026-09-20: 14 tests (1 passed = constants, 13 failed with "not implemented").
- [x] 0.6 [game-logic] Write `lib/game/golden.test.ts` covering every `[unit]` scenario in `specs/golden-button/spec.md` (with `place(P)` / `placeNull` stubs that count calls, `deepFreeze`); verify it fails with "not implemented". — done 2026-09-20: 16 tests (1 passed = constants, 15 failed with "not implemented").
- [x] 0.7 [game-logic] Write `lib/game/press.test.ts` covering the `[unit]` scenarios of requirement "Main-button press pipeline" in `specs/click-upgrades/spec.md`; verify it fails with "not implemented". — done 2026-09-20: 8 tests, all 8 failed with "not implemented".
- [x] 0.8 [game-logic] Rewrite `lib/game/click-value.test.ts` to the `[unit]` scenarios of `specs/clicker-core/spec.md` "Click value formula" (exact values, `creditClick` carry sequences) and `specs/click-upgrades/spec.md` "Click multiplier from upgrades" (v3 `S()` factory with merged `helpers` / `levels`); verify the new exact-value / carry / context scenarios fail on assertions or "not implemented". — done 2026-09-20: 21 tests (12 passed = unchanged Stage 1/2 rules, 9 failed: exact products on assertions, `creditClick` and the context scenarios).
- [x] 0.9 [game-logic] Rewrite `lib/game/shop.test.ts` to every `[unit]` scenario in `specs/shop/spec.md` (19-item catalog, reveal incl. speed-ups, prices incl. robot / factory / leveled, statuses incl. `maxed`, buying); verify it fails on assertions / "not implemented". — done 2026-09-20: 54 tests (37 passed = catalog data and Stage 2 rules, 17 failed: speed-up reveal and leveled prices on assertions, `getItemLevel` / feature- and leveled-upgrade branches with "not implemented").
- [x] 0.10 [game-logic] Rewrite `lib/game/helpers.test.ts` to every `[unit]` scenario in `specs/helpers/spec.md` (main spec "Game tick with fractional carry" scenarios kept, plus the MODIFIED / ADDED ones); verify the new ones fail. — done 2026-09-20: 18 tests (13 passed = kept tick scenarios and un-sped rates, 5 failed: `getHelperRate` "not implemented" and the speed-up tick assertions).
- [x] 0.11 [game-logic] Rewrite `lib/game/state.test.ts` to the MODIFIED `[unit]` scenarios in `specs/clicker-core/spec.md` (v3 `FRESH`, main-button click scenarios incl. the carry-free floor) and `lib/game/save.test.ts` to every `[unit]` scenario in `specs/game-persistence/spec.md` (v3 envelope, v3 validation incl. levels, `MIGRATIONS` with two entries, `migrateV1ToV2` returning `S2`, `migrateV2ToV3`, `RAW_V1` / `V2` / `V3` helpers); update only the `S()` fixture in `lib/game/skins.test.ts`; verify failures are assertions / "not implemented", never syntax or import errors. — done 2026-09-20: `state.test.ts` 12 tests (1 failed = carry-free floor assertion), `save.test.ts` 121 tests (81 failed on `serializeGame` / `validateGameState` / `migrateV2ToV3` "not implemented"), `skins.test.ts` 7 tests all passing (fixture only).
- [x] 0.12 [game-logic] Update fixtures in `components/game-store.test.ts`, `components/game-store.shop.test.ts` and `components/ShopBox.test.tsx` to v3 states (`S()` factory) and the expected envelope to `{ version: CURRENT_SAVE_VERSION, state: S(...) }`, no behavioural change; verify `pnpm typecheck` exits 0. — done 2026-09-20: `pnpm typecheck` exit 0; the 4 remaining component failures come only from the `serializeGame` / `getItemStatus` stubs, no fixture change of behaviour.
- [x] 0.13 [i18n] Add the 24 new keys (rows 45–68 of `specs/localization/spec.md`) to `uk` and `en` in `lib/i18n/dictionaries.ts` with value `""` (stub); update `lib/i18n/i18n.test.ts` to every `[unit]` scenario in `specs/localization/spec.md` (68-key set, 19 item names / descriptions via `t`, Stage 3 copy and interpolation, decimal formatting); verify key parity passes and the copy scenarios fail. — done 2026-09-20: `pnpm lint` exit 0; `pnpm test -- lib/i18n` exit 1 with 20 tests (10 passed, 10 failed); parity, 68-key count and the unchanged Stage 1 / 2 scenarios pass, every Stage 3 copy scenario fails on `""`.
- [x] 0.14 [e2e-qa] Write `e2e/add-upgrades-v2.spec.ts` with one `describe` per capability (crit, combo, golden-button, click-upgrades, helpers, shop, game-persistence, reduced-motion, page-decor — scenarios marked "New test" or living in the new capabilities) and one `test` per `[e2e]` scenario, selecting only by `data-testid`, role, `aria-*` / `data-*` attributes; local helpers `S()` (v3, merged helpers / levels), `S2()`, `seedStorage` (sessionStorage guard from `e2e/add-foundation.spec.ts`), `seedV2()`, `seedV3()`, `fixRandom(page, r)` (`page.addInitScript((v) => { globalThis.__dcRandom = () => v }, r)`; never override `Math.random`), `pauseClock(page)`, `fastClicks(page, n)` (click, `page.clock.runFor(100)` between clicks), `strictlyOverlap`, `animationNames(locator)`; fake clock per design D19. — done 2026-09-20: 39 tests (one per `[e2e]` scenario), `pnpm lint` exit 0, `npx playwright test e2e/add-upgrades-v2.spec.ts --list` exit 0 with "Total: 39 tests in 1 file"; not executed here (no dev server running, suite run deferred to 0.16).
- [x] 0.15 [e2e-qa] In `e2e/add-foundation.spec.ts` update only the expected envelopes of "Corrupted save in the browser" and "Reload keeps balance" to `{ version: 3, state: <v3 FRESH with balance / totalClicks> }`; in `e2e/add-shop-v1.spec.ts` update only "All items at 750 clicks" (15 ids), "One click short of the press" (14 items), "Buying a skin through the UI" and "Stage 1 player continues in Stage 2" (v3 envelopes) and "No catch-up for closed time" (9 state keys incl. `levels`), exactly as in the MODIFIED scenarios; change nothing else in those files. — done 2026-09-20: 2 envelopes in `add-foundation`, 5 spots in `add-shop-v1` (a local `S3()` helper builds the v3 envelope states); `pnpm lint` exit 0, `tsc --noEmit` reports no error in `e2e/`.
- [x] 0.16 [game-logic] Run `pnpm typecheck`, `pnpm lint` (both exit 0), `pnpm test` and `pnpm test:e2e` (both fail only on assertions / "not implemented" / missing elements); record exit codes and pass / fail counts in this task. — done 2026-09-20: `pnpm typecheck` exit 0, `pnpm lint` exit 0 (no warnings), `pnpm test` exit 1 with 356 tests in 21 files (188 passed, 168 failed; 13 files failed, 8 passed). Per file: `lib/game/save.test.ts` 121 (40 pass / 81 fail), `lib/game/shop.test.ts` 54 (37 / 17), `lib/game/click-value.test.ts` 21 (12 / 9), `lib/i18n/i18n.test.ts` 20 (10 / 10, task 0.13), `lib/game/helpers.test.ts` 18 (13 / 5), `lib/game/golden.test.ts` 16 (1 / 15), `lib/game/combo.test.ts` 14 (1 / 13), `lib/game/state.test.ts` 12 (11 / 1), `lib/game/press.test.ts` 8 (0 / 8), `lib/game/crit.test.ts` 6 (1 / 5), `components/game-store.shop.test.ts` 6 (5 / 1), `components/game-store.test.ts` 5 (4 / 1), `components/ShopBox.test.tsx` 3 (1 / 2); all other files green (`lib/game/decor.test.ts`, `lib/game/preferences.test.ts`, `lib/game/skins.test.ts`, `components/BalanceCounter.test.tsx`, `components/MainButton.test.tsx`, `components/ResetProgress.test.tsx`, `components/preferences-store.test.ts`, `scripts/tests-locked.test.ts`). Every failure is an assertion on a stubbed value or `Error: not implemented`; no syntax, import or type errors. `pnpm test:e2e` NOT run in this session (no dev server; the Stage 3 UI does not exist yet, so the whole `e2e/add-upgrades-v2.spec.ts` suite would fail on missing elements) — it must be run before the red commit 0.17.
- [ ] 0.17 [human] Commit stubs + tests as `test(add-upgrades-v2): add failing tests`; verify `git log --oneline --grep "^test(add-upgrades-v2)"` shows it and `pnpm tests:locked` prints `ok add-upgrades-v2`.

## 1. Game logic (lib/game) [game-logic]

- [ ] 1.1 Implement `getCritChance` and `rollCrit` in `lib/game/crit.ts` (no `random` call at level ≤ 0); verify `pnpm test lib/game/crit.test.ts` passes.
- [ ] 1.2 Implement `createComboState`, `getComboLevel`, `getComboMultiplier` (`1 + level / 10`), `registerComboClick` in `lib/game/combo.ts`; verify `pnpm test lib/game/combo.test.ts` passes.
- [ ] 1.3 Implement `rollGoldenInterval`, `createGoldenState`, `tickGolden` (clamp with `MAX_TICK_MS`, order bonus → visible → countdown, same object for effective elapsed 0), `catchGolden`, `getGoldenBonus` in `lib/game/golden.ts`; verify `pnpm test lib/game/golden.test.ts` passes.
- [ ] 1.4 Implement the exact `getClickValue` (`Number(product.toFixed(6))`, no integer rounding), `creditClick` and `getClickModifiers(state, context?)` in `lib/game/click-value.ts`, and the carry-free `clickMainButton` (adds `Math.floor(value)`) in `lib/game/state.ts`; verify `pnpm test lib/game/click-value.test.ts lib/game/state.test.ts` passes.
- [ ] 1.5 Implement `createClickRuntime` and `pressMainButton` in `lib/game/press.ts` (carry in / out via `creditClick`, balance + `credited`, totalClicks + 1, runtime returned by reference when the combo is unchanged); verify `pnpm test lib/game/press.test.ts` passes.
- [ ] 1.6 Implement `getHelperRate` and the speed-up aware `getHelperClicksPerSecond` in `lib/game/helpers.ts`; verify `pnpm test lib/game/helpers.test.ts` passes.
- [ ] 1.7 Implement in `lib/game/shop.ts`: `getItemLevel`, the speed-up reveal rule in `isItemRevealed`, leveled prices in `getItemPrice`, `maxed` in `getItemStatus` (priority owned → maxed → hidden → requires → unaffordable → available), buying of feature and leveled upgrades in `buyItem` (arrays in catalog order); verify `pnpm test lib/game/shop.test.ts` passes.
- [ ] 1.8 Implement v3 `serializeGame` (exactly the schema keys, incl. `helpers` and `levels` sub-keys), v3 `validateGameState` (design D15 + normalization) and `migrateV2ToV3` (design D16) in `lib/game/save.ts`; verify `pnpm test lib/game/save.test.ts` passes.
- [ ] 1.9 Run `pnpm test:mutation`; verify the score is ≥ 70 % for `lib/**`, adding new (not editing locked) tests for surviving mutants. (Not on the agent-loop allow-list — a human runs it before the `feat` commit.)

## 2. Dictionaries (lib/i18n) [i18n]

- [ ] 2.1 Fill the 24 new keys in `uk` and `en` with the exact copy from `specs/localization/spec.md` (U+2019 in `з’являється`); verify `pnpm test lib/i18n` passes.

## 3. Screen wiring (components/, app/) [ui-frontend]

- [ ] 3.1 Extend `components/game-store.ts` per design D17: module-level `runtime: ClickRuntime`, module-level main-click carry, `components/page-random.ts` exporting `pageRandom = () => (globalThis.__dcRandom ?? Math.random)()`, `press(nowMs)` (calls `pressMainButton` with the carry and `pageRandom`, commits the state, stores the runtime and carry, returns the `PressResult`), `catchGoldenButton()`, golden creation after a successful `golden-button` purchase and on the first tick when missing, `tick(elapsedMs, now, place)` also advancing `tickGolden` and publishing a runtime snapshot every tick, `resetGame` resetting the runtime and the main-click carry; decor placement in `DecorLayer` switched from `Math.random` to `pageRandom`; add tests in a new `components/game-store.upgrades.test.ts`; verify `pnpm test components` passes.
- [ ] 3.2 In `components/GameScreen.tsx` replace `clickMainButton(state, getClickModifiers(state))` with `press(performance.now())`, pass `crit` to the crit effect (4.1), and pass a `place` callback to `tick` that calls `placeDecor({ viewport, size: GOLDEN_SIZE, reserved, random: pageRandom })` with the reserved rects of design D8 (reuse the DecorLayer reserved-rect helper, adding `click-status`); verify click-upgrades e2e "Every Stage 3 factor combines in the browser" passes.
- [ ] 3.3 Wrap the balance area, the main-button wrapper and the click-status slot in `[data-testid="shake-layer"]` (design D12; nothing inside it may be `position: fixed` — move the floating +N layer to `absolute` or outside it). Add the click-status slot (design D13) in `components/MainButton.tsx`'s wrapper: `[data-testid="click-status"]` 160 × 64, absolutely positioned 24 px right of the button, vertically centered; inside it the combo meter `[data-testid="combo"]` (`combo.label`, `data-combo-level`, only while combo is owned and level > 0) and `[data-testid="golden-bonus"]` (`golden.bonus`, `Math.ceil(bonusMs / 1000)`); verify the combo e2e scenarios pass.
- [ ] 3.4 Add `components/GoldenButton.tsx`: `<button data-testid="golden-button">` fixed at `decorRect(position, GOLDEN_SIZE, viewport)`, `aria-label` `golden.catch`, z-index above decor, onClick `catchGoldenButton()`, an inner element for the pulse (never animate the button root); verify the golden-button e2e scenarios pass.
- [ ] 3.5 Add `click-status` to the decor reserved rects in `components/DecorLayer.tsx`; verify page-decor e2e "Decor avoids the click-status slot" passes.
- [ ] 3.6 Extend `components/HelperZone.tsx` with `helper-robot` / `helper-factory` (+ `-count`), order monkey → robot → factory, labels with `getHelperRate` per type, all inside the 288 × 96 zone; verify the helpers e2e scenarios pass.
- [ ] 3.7 Extend `components/ShopBox.tsx`: counts for every helper, `shop-level-<id>` (level ≥ 1) and `shop-maxed-<id>` (replaces the buy button) for leveled upgrades, owned label for feature upgrades; verify the shop e2e scenarios pass.
- [ ] 3.8 Update the pre-paint script in `components/BalanceCounter.tsx` to accept save versions 1, 2 and 3; verify "Seeded save is shown with locale formatting", "Stage 1 player continues in Stage 2" and "Stage 2 player continues in Stage 3" pass.
- [ ] 3.9 Make reset clear the runtime (combo meter, golden button, bonus, crit effect); verify game-persistence e2e "Reset clears Stage 3 progress and runtime" passes.

## 4. Motion and visuals (app/globals.css, components) [fx-animations]

- [ ] 4.1 Add `components/CritEffect.tsx` (or extend `MainButton.tsx`): on `crit`, render for 900 ms `[data-testid="crit-text"]` (`crit.text`), `[data-testid="crit-flash"]` over the button, `[data-testid="crit-burst"]` with 12 `[data-testid="crit-particle"]` (angles `i × 30°`), set `data-crit="true"` on the main button and retrigger the shake on `[data-testid="shake-layer"]` (never `<main>`); a new crit restarts the effect; all `aria-hidden`, `pointer-events: none`; verify crit e2e "Crit shows the full effect", "Effect ends and the button stays in place" and "Crit multiplies click upgrades" pass.
- [ ] 4.2 Add keyframes `crit-pop` (900 ms, scale + rise + fade), `crit-flash` (400 ms, gold overlay opacity), `crit-burst` (700 ms, particles fly out and fade), `crit-shake` (300 ms, ≤ 6 px translate on `[data-testid="shake-layer"]` only, `animation-name: none` before the first crit), `golden-pulse` (inner glow), `robot-press`, `factory-press`; animate only `transform` / `opacity`; confirm fixed elements (shop, reset, helpers, decor, golden button) do not move during a shake; verify reduced-motion "Full motion plays the Stage 3 helper and golden effects" passes.
- [ ] 4.3 Add `@media (prefers-reduced-motion: reduce)` and `[data-motion="reduced"]` rules: crit text / flash static (`animation: none`), no particles rendered (coordinate with 4.1), no shake, golden button and robot / factory elements `animation: none`; verify the remaining `specs/reduced-motion/spec.md` scenarios pass.
- [ ] 4.4 Draw the golden button (64 × 64 gold coin-like button), robot and factory helper visuals (emoji or inline SVG, pressing a small button like the monkey); verify visual consistency in both themes (human check in 5.4).

## 5. Verify and commit [human + agent loop]

- [ ] 5.1 Run `pnpm check` (typecheck + lint + tests:locked + test); record exit code, test count and the `ok add-upgrades-v2` line.
- [ ] 5.2 Run `pnpm test:e2e`; record exit code and pass count (add-foundation + add-shop-v1 + add-upgrades-v2 + smoke).
- [ ] 5.3 [human] Run `pnpm test:mutation`; record the score (≥ 70 %) and explain surviving mutants in the PR.
- [ ] 5.4 [human] Visual check in both themes and both motion modes (crit effect, combo meter, golden button, robot, factory), then commit `feat(add-upgrades-v2): crit, combo, golden button, robot, factory and speed-ups` after 5.1–5.3 are green.
